"""Backlog capture-tier package.

Distinct from ``fno.graph`` (the feature graph / ``fno backlog`` alias
target) and ``fno.inbox`` (cross-project messaging). This package owns
the capture tier *below* idea nodes: a markdown holding-pen of ``fu-XXXXXX``
items that triage promotes into real ``ab-XXXXXXXX`` graph nodes.
"""
