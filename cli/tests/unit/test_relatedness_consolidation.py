"""Consolidation-gate regression fixture: the pair the detector scores as unrelated (plan x-3bd3).

Frozen 2026-08-17 from the live node text of x-17ba, x-49c8, x-5756 (the real
three-node lock family) plus x-b917 and the four nodes its rollup leg wrongly
attracted (x-3bd3, x-c136, x-1fbb, x-b281). The two family members now carry
status "superseded" on the live graph and similar_nodes filters superseded
candidates, so the fixture freezes them as the live "idea" rows they were when
the pair was measured. Everything else is verbatim node text.

The pair of assertions that matter: at floor=_MIN_SCORE the family ranks 1-2
for the family probe (recall is excellent), and at the default _DEDUP_MIN_SCORE
the same call returns [] (the 0.30 floor truncates a correct ranking to
nothing). That is the defect change 1 fixes, pinned so a future tuning edit
that lowers the constant fails loudly here and forces a re-read of the plan's
evidence. A details-dropped _tokens is detected at fixture scale by the
x-5756-first rank pin, not by ejection from the 8-node top-5 (against the full
graph that edit pushed the pair to rank 31; eight nodes cannot reproduce that).
"""
from __future__ import annotations

import json

from fno.graph.relatedness import (
    _DEDUP_MIN_SCORE,
    _MIN_SCORE,
    _score,
    _tokens,
    similar_nodes,
)

# Frozen verbatim from ~/.fno/graph.json; do not hand-edit (regenerate via the
# graph, or the pinned scores below drift for no visible reason).
_FIXTURE_JSON = r"""[
  {
    "details": "REPORTED 2026-08-15 by the x-7496 worker: implementation done, four atomic commits, tests green, and the PR could not be opened because the repo-wide preflight lock has been contended for hours. Each holder finishes in 20 minutes to over an hour and a different session takes it before the waiter does.\n\nMEASURED, same machine, 18:12Z. The holder is pid 65902 running scripts/ci/preflight.sh, alive, 54 minutes in, recorded in .git/.preflight.lock.d/holder as pid, start time, host and sha. Thirteen preflight processes were running at once. Three of them were HANDROLLED RETRY LOOPS in different sessions: preflight_full_loop.sh at 2h49m, preflight-retry.sh at 1h55m, and run_preflight2.sh at 51m.\n\nTHE SELF-INFLICTED PART. The reporting worker was running TWO copies of its own retry loop, both nearly three hours old. So a session complaining about starvation was contributing two of the racers. I killed all three loops.\n\nWHY IT STARVES. The lock is a bare lock directory with a holder file. There is no queue, no ticket, no fairness and no wait ordering. Every release is a thundering herd, and the winner is whichever waiter's sleep timer happens to align. Under N waiters an unlucky session can wait indefinitely while others cycle through, which is exactly the reported symptom. Retry loops do not improve a waiter's odds, they raise contention for everyone.\n\nTHE WAIT WAS ALSO POINTLESS IN THIS CASE, and that is the sharper finding. The x-7496 branch is 50 commits BEHIND origin/main. A green preflight on that base attests a tree that will not be merged, so the run being waited for could never have produced usable evidence. The rebase has to happen first and invalidates the result anyway.\n\nTHE ESCAPE HATCH ALREADY EXISTS AND IS SANCTIONED. cli/src/fno/pr/_preflight.py:715 returns (False, 'explicit-skip') when FNO_SKIP_PREFLIGHT=1. CI re-runs the same checks server-side on the PR, so local preflight is an optimisation that saves a red PR, never the gate itself. A finished feature blocked for hours on an optimisation is the wrong trade.\n\nFIX, in order of value. First, fairness: give the lock a FIFO ticket so waiters are served in arrival order and a session cannot be lapped. Second, reuse: .git/.preflight-attestation already records sha, mode, verdict and time, so a waiter whose base sha already has a green FULL attestation should read it instead of queuing to recompute it. Third, refusal: preflight should refuse to run at all when HEAD is behind the base, since the result cannot attest the merge head. Fourth, guidance: the acquire path should print the holder's pid, start time and elapsed, plus the FNO_SKIP_PREFLIGHT escape, so a waiter can make an informed choice rather than looping blind.\n\nDO NOT SOLVE THIS WITH A RETRY LOOP. Three of them were running tonight and they are the contention. Any doc or skill text that suggests polling for the lock should be changed to name the ticket or the skip.",
    "domain": "code",
    "id": "x-17ba",
    "slug": "repo-wide-preflight-lock-starves-waiters",
    "status": "done",
    "title": "the repo-wide preflight lock starves waiters: FIFO fairness, stale-base refusal, holder guidance",
    "type": "feature"
  },
  {
    "details": "Filed per king's ruling on PR 839 (x-01ae / x-474a): a shared preflight lock held 15+ minutes blocked this session from running preflight before push, and the king called a 15-minute unexplained lock hold itself a defect worth filing.\n\nInvestigated scripts/ci/preflight.sh's lock (mkdir at $COMMON_DIR/.preflight.lock.d): it IS liveness-gated, not mtime-staleness-gated. acquire_lock reads the holder's pid from the holder file and steals only when kill -0 on that pid fails (dead process), via an atomic rename-then-verify (never rm -rf + mkdir, to avoid two racers running preflight against the same shared worktree). This is NOT the same defect class as x-474a's mutex zombie-lock bug - there is no mtime-based steal window to backdate here, because liveness is checked directly against the OS, not inferred from age.\n\n[OVERTURNED 2026-08-13, see the correction at the foot of this node] So the 15-minute wait observed this session was very likely a genuinely live, genuinely slow preflight run in another worktree, correctly protected from a concurrent steal - not a broken lock. The actual gap: while a worktree waits behind the lock, it has no way to see which session holds it beyond `cat .preflight.lock.d/holder` (pid/host/sha, no node id, no progress, no ETA), and no fast-fail or priority path for a worktree whose own preflight would be fast. Every worktree in the repo serializes behind whichever one got there first, however long that one takes.\n\nDid not identify the specific session/node that held the lock this run (not captured at the time); a future occurrence should capture `cat $COMMON_DIR/.preflight.lock.d/holder` immediately on the \"lock held\" message rather than after the fact.\n\nSuggested shape, not prescribed: surface the holder's node id (if resolvable) and elapsed time in the \"lock held\" message so a waiting session can judge whether to wait or ask; consider whether preflight's slow legs (full rust suite) belong behind a separate, coarser lock than the fast legs, so a fast preflight isn't queued behind a slow one it doesn't share a worktree with.\n--- ESCALATION 2026-08-13, measured by the king. This is not a visibility gap alone. ---\n\nThe p3 framing understates it. The steal predicate CANNOT FIRE for the most likely failure mode.\n\nMEASURED, live, while PR 839 waited on this gate.\n\n  PID    ELAPSED   CPU-TIME  NI  STAT  COMM\n  7819   53:10     0:00.04   5   SN    bash      <- lock holder\n  26132  51:33     0:00.00   5   SN    bash\n  26136  51:42     0:00.02   5   SN    uv\n  26143  51:42     0:00.31   5   SN    Python\n\nThe whole chain is alive, niced to 5, and sleeping. The Python leaf accumulated 0.31 SECONDS OF CPU IN 51 MINUTES. It is not computing. It is blocked on something.\n\nWHY THE LOCK NEVER FREES. The steal condition is `kill -0` PID death. A blocked-but-alive holder passes that check forever. So the one process most in need of being stolen from is the one the predicate is guaranteed to protect.\n\nA dead holder is the case the design handles. A hung holder is the case that actually happens, and it is indistinguishable from a slow one under a loaded fleet, because both are alive.\n\nTHE NICE LEVEL MAKES IT WORSE, not better. Niced to 5 under ten-plus concurrent workers means the holder is starved of CPU exactly when the fleet is busiest. So contention for the gate peaks at the same moment the holder is least able to finish and release it.\n\nCONSEQUENCE OBSERVED. PR 839 could not run its own preflight for over 50 minutes. Its worker pushed on manual validation instead and disclosed it. So the gate's failure mode is not a stall, it is workers routing around the gate.\n\nWHAT THE FIX NEEDS, beyond the visibility this node already names.\nA liveness predicate that measures PROGRESS, not existence. Accumulated CPU time, or a heartbeat the holder must refresh, distinguishes working from wedged. `kill -0` cannot.\nA ceiling. A gate lock held past some bound is stolen or the waiter is told to proceed with a recorded exception, rather than every waiter deciding for itself and disclosing it in a PR body.\n\nRELATED SHAPE, already in this project's corpus: liveness probes have lied, and only the live lockfile and the transcript stayed truthful. Here the lockfile is truthful about EXISTENCE and silent about PROGRESS, which is the thing a waiter actually needs.\n\n--- CORRECTION 2026-08-13. The paragraph marked OVERTURNED above is wrong, and the cause is now measured. ---\n\nThe holder was not slow. It was starved, by orphaned load from the same fleet.\n\nMeasured the same night, filed as x-4825. One session left 73 `yes > /dev/null` processes at PPID 1, 797 percent CPU combined, 71 core-hours over 7.5 hours. `yes` writing to /dev/null never receives SIGPIPE, so the normal death path for an abandoned pipeline does not exist and the load never ended on its own.\n\nThe lock holder was niced to 5 under that load. It accumulated 0.31 SECONDS of CPU in 52 minutes. It was alive the whole time, so `kill -0` passed the whole time, so the lock never freed.\n\nThe chain closes on itself. The session waiting on the gate was 1c58b122, the same session whose own test fixture created the load. It starved its own gate and neither end was visible from the other. `pkill -x yes` freed the cores and the wedged preflight completed within seconds.\n\nWHAT THIS CHANGES FOR THIS NODE. The original body treated a slow-but-working holder as the likely case and queue visibility as the fix. A wedged holder is the case that actually happened, and no amount of ETA reporting frees the lock. The steal predicate has to measure PROGRESS: accumulated CPU time over a window, or a heartbeat the holder must refresh. Existence cannot tell working from wedged, and under a loaded fleet a starved holder and a busy one look identical to `kill -0`.\n\nVisibility is still worth having and is no longer the headline. A ceiling is also needed: a gate lock held past a bound is stolen, or the waiter is told to proceed with a recorded exception, rather than each waiter deciding alone and disclosing it in a PR body as PR 839's worker did.\n\nPREVENTION OF THE CAUSE IS SEPARATE AND ALREADY PLANNED. x-4825 stops the orphaned load at creation and sweeps for what survives. It does not fix this lock. A steal predicate that cannot distinguish wedged from working is wrong on an idle machine too, with no orphans anywhere.\n\n--- FIELD NOTE 2026-08-13, measured while blocked by this very lock. ---\n\nA second session held the preflight lock for 28 minutes while this one waited. It was NOT wedged, and the way to tell matters for the fix.\n\n  PID    ELAPSED  CPU-TIME  NI  COMM\n  94359  27:54    0:00.03   0   bash     <- lock holder\n  75276  24:22    0:00.00   0   bash\n  75312  24:26    0:00.02   0   uv\n  75391  24:29    0:00.40   0   Python\n  70618  00:11    0:00.14   0   bash     <- leaf, ELEVEN SECONDS OLD\n\nBy accumulated CPU the holder looks identical to the starved holder measured in the escalation above: 0.03 seconds in 28 minutes. By child turnover it is obviously alive, because the bottom of its process tree is 11 seconds old and keeps being replaced.\n\nSO CPU TIME ALONE IS THE WRONG PROGRESS SIGNAL for this holder shape. A shell that orchestrates steps burns almost no CPU itself no matter how well the run is going, because all the work happens in short-lived descendants. A steal predicate keyed on the holder's own CPU time would rob a healthy run.\n\nA third option beside CPU time and a heartbeat: DESCENDANT TURNOVER. Sample the youngest process in the holder's tree, or the set of descendant pids, twice. A wedged holder's tree is static. A working one's churns. It needs no cooperation from the holder, which a heartbeat does.\n\nWhatever predicate lands, test it against BOTH shapes: the starved holder from the escalation, and this healthy orchestrator. They are indistinguishable by `kill -0` and by holder CPU time, and opposite by descendant turnover.\n\n--- THE FUTURE OCCURRENCE THIS NODE ASKED FOR, 2026-08-14. Holder captured live, as instructed. ---\n\nThis node told a future session to capture the holder file immediately rather than after the fact. Done. Two separate holds tonight, and the second blocked THREE workers.\n\nFIRST HOLD, captured at the moment of contention:\n  pid=83110 started=2026-08-14T21:06:24Z host=BB16s-MBP sha=79a800a990ae3a9f5ba8889411a0170d72aaa74e\nIt ran 46 minutes. Parent was a codex session, pid 42817, alive 2h45m. Blocked c4d79fdb on x-3a91 (nine commits, 1385 tests green) and 39d036f0 on x-cc90 (7467 tests green). Neither could open a PR because fno pr evidence-check wants a passed preflight receipt at exact HEAD.\n\nSECOND HOLD: pid 21133, 33-plus minutes, blocked the same worker's retry. A third worker, c8e55e47, reported three different holders in a row and never got a clean run at all; it opened its PR without one on a king ruling.\n\nTHE NODE'S OWN OVERTURNED PARAGRAPH IS CONFIRMED CORRECT. The holder was genuinely live and genuinely slow, not wedged. Proof: sampling the process chain twice, six minutes apart, showed the deepest child move from bash scripts/tests/test_do_provenance_stamp.sh to bash tests/hooks/test_size_profile.sh. So the lock behaved exactly as designed and the defect is throughput, precisely as this node already says.\n\nNEW SUB-FINDING, and it cost two workers a wrong diagnosis. Both read 0.0 percent CPU across the whole chain and concluded wedged. It was not: the smoke legs are shell tests that wait on subprocesses, so CPU is a useless liveness probe here. The probe that works is two samples and asking whether the DEEPEST CHILD changed. That belongs in the script's own lock-held message, where it cannot drift, not in a doc.\n\nALSO LITTER, same subsystem. ~/.fno/.preflight-receipt-locks/ holds ten stale per-SHA lock dirs, oldest 2026-07-27. Harmless by design, since each reaps on a dead holder, but nothing ever removes them. Sweep them on the same leg.\n\nDIRECTIONS, and this node owes a choice rather than all three. Per-worker hermetic worktree, trading disk for parallelism when disk is already at 51GB after tonight's sweep. Or shard the suite so a receipt covers only the legs a diff can affect. Or keep the lock and make waiting cheap: publish queue position and an ETA derived from the running suite's own progress. Prefer the third first; it is smallest and it makes the other two measurable. A queue with no visibility cannot tell you whether parallelism is worth the disk.\n\nDUPLICATE FOLDED IN. x-5756 was filed 2026-08-14 for this same defect by a king who did not search the backlog first. Its content is above; it is superseded by this node.",
    "domain": "code",
    "id": "x-49c8",
    "slug": "global-preflight-lock-has-no-queue",
    "status": "idea",
    "title": "Preflight lock steals on kill -0 death only, so a starved-but-alive holder never frees it: needs a progress-based steal and a ceiling",
    "type": "feature"
  },
  {
    "details": "Measured 2026-08-14. scripts/ci/preflight.sh serializes through ONE shared hermetic worktree per host, behind an atomic-mkdir lock at COMMON_DIR/.preflight.lock.d. Holder pid 83110 ran 46 minutes. Two workers sat idle behind it the whole time: c4d79fdb with nine commits and 1385 passing tests on x-3a91, and 39d036f0 with 7467 passing tests on x-cc90. Neither could open a PR, because fno pr evidence-check wants a passed preflight receipt at exact HEAD.\n\nTHE LOCK IS NOT BROKEN. It is doing its job. The dead-holder reap works, the steal-then-verify rename is careful, and the per-SHA cache means a second caller on the same SHA exits 0 before contending. The defect is that throughput is one worker at a time per host while the fleet runs thirteen.\n\nSECOND FINDING, and it cost two workers a wrong diagnosis. Both read 0.0 percent CPU across the whole process chain and concluded wedged. It was not. The smoke legs are shell tests that wait on subprocesses, so CPU is a bad liveness probe here. The probe that works is sampling twice and asking whether the DEEPEST CHILD CHANGED: it moved from bash scripts/tests/test_do_provenance_stamp.sh to bash tests/hooks/test_size_profile.sh across six minutes. Put that two-sample rule in the script's own lock-held message, so the next blocked caller reads it instead of inferring wedged from CPU. Runtime text beats a doc here because it cannot drift.\n\nDIRECTIONS, and the node owes a choice rather than all three. Give each worker its own hermetic worktree, which trades disk for parallelism and disk is already at 90GB. Or shard the suite so a receipt covers only the legs a diff can affect. Or keep the lock and make waiting cheap by publishing a queue position and an ETA from the running suite's own progress.\n\nPrefer the third first: it is the smallest and it makes the other two measurable. A queue with no visibility cannot tell you whether parallelism is worth 30GB.\n\nALSO LITTER. ~/.fno/.preflight-receipt-locks/ holds ten stale per-SHA lock dirs, oldest 2026-07-27. They are harmless by design, since each is reaped on a dead holder, but nothing ever removes them. Sweep them on the same leg.",
    "domain": "code",
    "id": "x-5756",
    "slug": "preflight-serializes-every-worker-one-host",
    "status": "idea",
    "title": "preflight serializes every worker on one host lock: two PRs idle 45 minutes behind one suite",
    "type": "feature"
  },
  {
    "details": "Epic from the 2026-07-28 Lanzone Morgan call (transcript: raw/meetings/20260728-langonemorgan.md). They did not ask for analysis. They asked for sourced raw material that removes tab-juggling hours, and provenance that survives a foundation objection (Jim on Ernie Tosh: accurate work, excluded for lack of foundation).\n\nThree layers, ordered so each makes the next cheaper: (1) export what the pipeline already holds, which is all registry edits in scripts/dossiers/data_appendix.py against payloads already on disk; (2) the retrieval record (URL, route, capture ts, content hash, stored copy) with the binding constraint that no model may sit in the fetch path, enforced by a twice-run byte-identity test, because model-mediated retrieval is a judgment and a judgment is not a business record; (3) resolution and derived math (CA SOS officers/agent/address, residency-window cost math, county/state/national benchmarks).\n\nCross-cutting: every deliverable is a reconstruction of a PAST WINDOW (the harm block), never current state. Jim asked for the window three times without using the word.\n\nARTIFACTS:\n- Design and evidence: internal/etl/plans/20260728-lanzone-morgan-prove-it-provenance-x-b917.md\n- Flow doc with mermaid dependency chart: internal/etl/backlog/lanzone-morgan-epic.md\n- Obsidian board over all 14 children: internal/etl/backlog/lanzone-morgan-epic.base (views: Flow / Ready now / Blocked / By priority / Board)\n- One note per child in internal/etl/backlog/, carrying status, blocked_by and blocks as wikilinks, plus rank and wave.\n\nSEQUENCING: 8 children unblocked (wave 0), 6 gated (wave 1). x-adf3 has the widest fan-out and is the highest-leverage single item. ab-874b668f is the long pole and the only node with an adversary. Run x-71a4 before the SOS work pays off: HCAI Section 10.4 names counterparties in full legal form, resolving the HCRIS 36-char truncation without inference and without touching the WAF.\n\nFREEZE: nothing derived from hcris_snf_related_party amount_charged / amount_not_allowed / amount_allowed ships until x-3adb confirms the A-8-1 column semantics. The names are unsourced and the data contradicts them on 41.5% of rows.\n\nOUT OF SCOPE: pricing, services-vs-product positioning, expert testimony, the bank/judgment trace.\n\n=== CATALOGUE MOVED, king-x-b917-wave16, 2026-08-06 ===\n\nThe defect catalogue (entries 15 to 31, plus the PR #874 evidence attestation and the wave-13b\nking's-own-instrument-failures section) NO LONGER LIVES HERE. It was 48,657 bytes of this field.\nIt now lives with entries 6, 9, 13 and 14 in:\n\n  internal/etl/troubleshooting/20260731133000_king-x-b917-wave8_null-read-as-pass-generator.md\n\nRead it there. Do not re-paste it into a handoff; link to it. Verified at move time: the file\ncontains the block head, the block tail, the whole block byte-for-byte, and its own prior\nentries, checked before this field was truncated.\n",
    "domain": "code",
    "id": "x-b917",
    "slug": "lanzone-morgan-prove-it-deliverables-provenance",
    "status": "ready",
    "title": "Lanzone Morgan prove-it deliverables and the provenance layer",
    "type": "epic"
  },
  {
    "details": "OPERATOR PROPOSAL 2026-08-15: should a grooming agent combine nodes into one PR, or should every blueprint check for similar nodes and either append to another node or absorb it as waves of one deliverable?\n\nTHE DAEMON HALF ALREADY EXISTS AND SHOULD NOT BE DUPLICATED. fno backlog groom runs a daily levers-only pass, and its allowlist already contains supersede --replaces --reason, which is exactly the manual call that consolidated x-49c8 into x-17ba tonight. It is deliberately proposal-only for dedup and restricted to reversible levers, which is what makes an unattended pass safe. It explicitly does not plan features or open PRs. A second autonomous consolidator would be a competing writer on the same graph.\n\nTHE DETECTION IT RELIES ON DOES NOT WORK. Measured 2026-08-15 on the live graph: fno backlog maintain reports dedup-groups 0. Zero. It did not find x-17ba and x-49c8, which are the same lock in the same file, and it did not find that x-49c8 had already superseded x-5756, so a three-node chain about one lock was invisible to it while a human spotted it in one sentence.\n\nWHAT IT EMITS INSTEAD IS ACTIVELY MISLEADING. 20 rollup candidates, every score between 0.20 and 0.21, and 17 of them pointing at the single attractor node x-b917. Among them: x-c136 (a resume verb), x-1fbb (an agent-list column) and x-b281 (a mail lock), three unrelated nodes filed the same evening, all proposed as children of one epic. A 0.20 similarity is noise, and an attractor that swallows a fifth of the backlog is a clustering artifact, not a finding.\n\nSO THE JUDGMENT CANNOT LIVE IN A TITLE-LEVEL SCAN. This is the corpus entry about delegating judgment to a subprocess on a truncated context, demonstrated: the scan sees titles and slugs, decides with confidence, and is wrong in both directions at once, missing the real family and inventing twenty false ones.\n\nPUT IT IN BLUEPRINT, WHICH IS THE ONLY PLACE WITH THE CONTEXT. A blueprint has read the node's full details, its plan seed, and the code it touches. That is what let a reader say x-49c8 is wave B of x-17ba: the steal predicate and the fairness queue are the same lock, and fairness alone cannot help behind a holder that never finishes. No title comparison reaches that.\n\nblueprint already has the hook. skills/blueprint/SKILL.md:140 runs fno think inspect --json for grounding and that receipt ALREADY REPORTS DUPLICATE CANDIDATES. Nothing consumes them for consolidation today; grep for duplicate, similar, supersede, absorb across the skill returns only that line, an unrelated suffix rule, and the claims-ingestion gate.\n\nTHE STEP. Before writing the plan, blueprint resolves candidates and picks exactly one of three outcomes, recording which and why in the plan: ABSORB the other node as a wave of this deliverable and supersede it; APPEND this node's content to the other and stop, producing no second plan; or PROCEED ALONE with the reason stated. Silence is not an option, because the failure mode is a plan written as if the sibling did not exist.\n\nWHY THIS IS THE RIGHT CHOKEPOINT. The project contract is already one plan, one PR, one node. Whether a second node belongs inside this PR is a planning question, and it is asked exactly once per deliverable rather than daily across 382 ready nodes.\n\nKEEP IT REVERSIBLE. groom's supersede is undone by unsupersede, and a blueprint-time absorption must be too, so a wrong call costs a command rather than a rewrite.\n\nVERIFY. A test that a blueprint over one of a known pair records an absorb or append decision rather than silently planning alone. A test that the three outcomes are exhaustive, so no path writes a plan with no decision recorded. And a regression fixture built from x-17ba and x-49c8, since that pair is a real family the current detector scores as unrelated.\n\nRELATED: this does not fix maintain's clustering. The 0.20 attractor behaviour deserves its own look, and until then its rollup output should not be applied.",
    "domain": "code",
    "id": "x-3bd3",
    "slug": "blueprint-must-consolidate-before-it-plans",
    "status": "in_progress",
    "title": "blueprint consolidates before it plans: a pre-write gate with exactly one recorded outcome",
    "type": "feature"
  },
  {
    "details": "OPERATOR-REPORTED 2026-08-15: nine background sessions sat at \"Needs input, rate limited\" for hours. The king could not resume any of them and burned many turns failing. The operator had to point out repeatedly that resuming was possible.\n\nTHE WORKING RECIPE, verified. t-xe21e went from state blocked to busy/working:\n\n  { sleep 6; printf 'continue\\r'; sleep 8; } | timeout 50 script -q /dev/null claude attach <shortid>\n\nTHREE THINGS MAKE IT WORK AND EACH ONE FAILED ALONE.\n\nFirst, script -q /dev/null ALLOCATES A PSEUDO-TTY. claude attach and fno agents resume both refuse without one. From a non-interactive shell they print \"Attaching...\" and exit, which reads as a no-op rather than a refusal.\n\nSecond, ATTACHING IS NOT ENOUGH. An attach prints \"Waking session <id>\" and revives the process, and the session then sits at its prompt doing nothing. It needs INPUT to continue.\n\nThird, THE INPUT NEEDS A CARRIAGE RETURN AND A DELAY. printf with a newline types the text into the prompt and never submits it: the message was visibly sitting on the input line unsent. A \\r submits it. The leading sleep lets the TUI finish initialising, otherwise the keystrokes arrive before the input exists.\n\nWHY EVERY OTHER PATH FAILED, all measured tonight.\n\nfno agents resume: prints \"Attaching...\", needs a TTY, exits 0 having done nothing.\nfno mail send <name>: \"queued (durable) [live-miss]\". Never delivered.\nfno mail send <full-session-uuid>: forks a NEW incarnation rather than resuming, and that fork showed as Failed in the fleet view.\nclaude agents --help: exposes no reply or send option at all. Only stop, rm and attach exist as CLI verbs.\n\nA SEPARATE DISCOVERY THAT COST HOURS. claude agents --json OMITS stopped and completed sessions. It listed 57 rows while --all listed 133. Every reap sweep the king ran was blind to exactly the rows it was trying to reap, and reported success over an empty set.\n\nWHAT SHOULD EXIST. fno agents resume <name> must allocate its own pty, attach, send an optional message, and confirm the state moved from blocked to working. The confirmation is the part that matters: tonight every resume attempt exited 0 while the row stayed blocked, which is the absence-as-success shape this repo already has a pitfalls entry for.\n\nSECOND DELIVERABLE: fno agents rm should delegate to claude rm for claude rows, and any listing it reads must pass --all.\nCORRECTION 2026-08-15, TWO HOURS LATER. The recipe recorded above is INCOMPLETE and fails on a blocked session. Two more things are load-bearing. The operator supplied both after watching the king fail repeatedly.\n\nFIRST, BRACKETED PASTE EATS THE CARRIAGE RETURN. The session terminal runs with bracketed paste enabled. Text and a trailing \\r sent in ONE write arrive as a paste, so the \\r inserts a NEWLINE into the input rather than submitting it. The proof was visual: an attach showed THREE stacked copies of the wake message sitting unsent on the prompt line, one per attempt. The fix is three SEPARATE writes with delays: \\x15 to clear the line, then the text, then \\r alone.\n\nTHIS EXPLAINS WHY stopped SESSIONS WORKED AND blocked ONES DID NOT. Three stopped sonnet sessions went stopped to working on the first try with the single-write form. Every blocked session stayed blocked. The difference was never the recipe. It was that a stopped session takes a different input path than a session already sitting at a populated prompt.\n\nSECOND, AND THIS IS THE ONE NOBODY WILL GUESS. A routed session must resume with ITS OWN ENV. The registry row carries route_settings_path, for example ~/.fno/route-settings/cdb56ee04b4840cd.json, and that file holds ANTHROPIC_BASE_URL pointing at api.z.ai plus the model mapping and the auth token. Attach inherits the CALLER's environment. So a GLM session resumed from a plain shell comes back pointed at Anthropic rather than z.ai.\n\nclaude attach TAKES NO OPTIONS AT ALL, so it cannot be told. Only the environment can carry it. claude agents accepts --settings, but that is the TUI launcher, not the attach.\n\nTHE COMPLETE RECIPE, verified moving t-x48a5 from blocked to working:\n\n  read route_settings_path for the session from ~/.fno/agents/registry.json\n  export every non-empty key from that file's env object\n  { sleep 7; printf '\\x15'; sleep 1; printf '%s' \"$MSG\"; sleep 2; printf '\\r'; sleep 9; } \\\n    | timeout 60 script -q /dev/null claude attach <shortid>\n  then re-read claude agents --json --all and assert the state moved to working\n\nA working implementation lives at /Users/bb16/.claude/jobs/119e3c52/tmp/wake.sh. It resolves the id and the route settings from the registry by name, emits the exports through python and evals them so no credential is ever echoed, retries once, and prints before to after.\n\nSO fno agents resume MUST DO FIVE THINGS, not two. Allocate a pty. Load the row's route settings into the child env. Clear the input line. Send the message and the submit as separate writes. Verify the state moved. Four of those five are missing today, and the fifth, verification, is what makes the other four detectable.",
    "domain": "code",
    "id": "x-c136",
    "slug": "fno-agents-resume-cannot-resume-anything",
    "status": "idea",
    "title": "fno agents resume cannot resume anything: it needs a pty, an attach, a message and a carriage return, and confirms nothing",
    "type": "feature"
  },
  {
    "details": "OPERATOR PROPOSAL 2026-08-15: make the fno agent list more durable and more accurate than what claude and codex report, so we can tell whether something is actually live and actually still being updated, and see the last time an event happened on a transcript.\n\nWHY THIS IS RIGHT. Over one session every other instrument lied, and each lie cost real work.\n\nThe fno registry lied in BOTH directions. It called t-x7496s exited while that session was demonstrably working and holding four unpushed commits, and it called wake-c26a1d50 live after the session was gone. Because it read claim None on x-7496 I dispatched a second worker onto a node that already had one, which is the duplicate the operator has now flagged twice.\n\nclaude agents --json hid more than half the fleet: 58 rows against 123 with --all. Every reap sweep run without that flag was blind to exactly the rows it was inspecting and reported success over an empty set.\n\nfno agents peek and fno mail send both hang at EXIT=124, so neither can answer a liveness question at all right now.\n\nLock files answer nothing: 2590 of them, 0 bytes each, and lsof shows no holder.\n\nThe transcript was right every time. It gave the 429 and its reset stamp for two blocked workers, the live progress line for t-xc136 two minutes old, proof that an injected message landed, and the fact that a session the registry called exited was working.\n\nTHE MEASUREMENT, taken 2026-08-15 across 130 rows joined to their transcripts. Minutes since last transcript write, by claude state: working n=19 median 2.6 max 172.7; stopped n=21 median 215.2; blocked n=31 median 1839.6; done n=56 median 3403.3; failed n=3 median 4831.2. A working session's transcript is a median of 2.6 minutes old. Everything else sits between 3.5 hours and 3.4 days.\n\nSO IT DISCRIMINATES, BUT IT IS NOT A CLASSIFIER ALONE. The tails overlap: a just-stopped row reads 0.9 minutes and a wedged worker reads 172 minutes while still claiming working. Do not replace the existing status with a derived one. Add the column the stores do not have and let a disagreement be visible rather than resolved silently.\n\nSCOPE. fno agents list gains last_event_at, sourced from the transcript file, plus the age derived from it. The row also carries the last line's shape, because a 429 is the difference between blocked-and-waking and blocked-until-a-reset-time, and the reset stamps are Singapore time. Where the harness state and the transcript age disagree, SHOW BOTH. A working row whose transcript is hours old is the exact signal that has been missing all day, and it is the same signal fleetwatch approximated badly using registry last_message_at, which is absent on many rows.\n\nFINDING THE TRANSCRIPT IS THE ONE NON-OBVIOUS PART. It lives under ~/.claude/projects/<slug>/<session-id>.jsonl, and the slug is keyed to the WORKTREE path, not the repo. A glob built from the repo name misses every worktree worker. Resolve by session id across all project dirs.\n\nHONEST LIMITS. 5 rows carry no state and no transcript, and 1 working row has no transcript at all, so the column must render as unknown rather than as fresh. An absent reading is not evidence of health, and reporting it as such is the failure this node exists to end.\n\nRELATED, NOT DUPLICATED. x-d4a6 is the narrower bug where a dead-pid bg worker with a stale non-idle live_status skips the truth_status fallback; this node supplies the truth source that fallback needs. x-e520 owns reaping and the claude rm delegation. x-c136 owns resume, which must read the same transcript tail rather than probe.",
    "domain": "code",
    "id": "x-1fbb",
    "slug": "fno-agents-list-must-carry-last",
    "status": "done",
    "title": "fno agents list must carry last_event_at and last_message from the transcript",
    "type": "feature"
  },
  {
    "details": "OPERATOR-REPORTED 2026-08-15: \"it seems a bunch of sessions are deadlocked and cannot mail you?\"\n\nTHE REPORT THAT DIAGNOSED IT, from worker bp-xc692, verbatim: \"fno mail send could not get your agent lock: three attempts printed Waiting for agent king-footnote-g4 lock, timed out at 30s, and exited 0 with no receipt line. Nothing queued either (fno mail sent --unclaimed is empty). Treat that exit 0 as the lie it is.\" It had to reach the king over the harness cross-session channel instead.\n\nTHE LOCK, MEASURED. /Users/bb16/.fno/agents/locks/king-footnote-g4.lock. ZERO BYTES. mtime Fri Aug 14 00:17:59, which was over 31 hours before the report. A zero-byte file carries no holder pid, no session id and no timestamp inside it, so nothing about it can ever be proven live or dead by a reader.\n\nTHREE SEPARATE DEFECTS IN ONE OBSERVATION.\n\nOne, THE LOCK NEVER EXPIRES. It survived 31 hours across many sessions. Nothing swept it.\n\nTwo, THE LOCK CARRIES NO HOLDER. Zero bytes means a waiter cannot distinguish a live holder from a corpse. Compare claim lockfiles, which at least record a pid. This does not.\n\nThree, AND THIS IS THE WORST. A 30-second wait that gives up EXITS 0 WITH NO RECEIPT AND QUEUES NOTHING. The sender believes the mail was sent. The recipient never gets it. There is no durable copy to drain later. That is silent message loss presented as success, and it is the same absence-as-outcome shape the pitfalls corpus already names.\n\nSCALE OF THE SURROUNDING PILE, same measurement pass. 2565 files in ~/.fno/agents/locks and 1373 in ~/.fno/locks. 3938 lock files total. This is the same never-evicts pattern as x-aeeb (claims never GC, 1645 of 1673 held dead pids) and x-73b9 (943 pr-watch entries against 6 open PRs) and the 556 context-nudge latches in x-d339.\n\nIMMEDIATE ACTION TAKEN. The king removed its own stale lock after backing it up. That is a one-off repair, not a fix.\n\nTHE FIX HAS THREE PARTS, one per defect.\n\nOne. A lock file must record its holder: pid, session id, and acquire time. A zero-byte lock is unfalsifiable by construction.\n\nTwo. A lock must expire, and the waiter must be able to break a provably dead one rather than waiting the full timeout on a corpse.\n\nThree, AND THIS IS THE PART THAT CANNOT BE SKIPPED. A send that fails to acquire the lock must NEVER exit 0. It either queues durable, or it exits non-zero and says so. Silent loss is worse than a refusal, because the sender moves on believing the message landed. Two workers lost reports to this tonight before one of them thought to use a different channel.\n\nVERIFY WITH A POSITIVE MARKER. A test that holds the lock, sends a mail, and asserts the message is retrievable afterward. Asserting a non-zero exit alone is not enough: the message has to exist somewhere.\n\nSECOND SPECIMEN, 2026-08-15, and this one proves total loss rather than silent delay.\n\nWorker t-xb2bf finished the yard sprite design doc for x-b2bf and tried three times to mail the king at 119e3c52. All three attempts returned timed out waiting for agent 'king-footnote-g4' lock. The worker stopped retrying, which was the correct call, and reported the failure into its own pane instead.\n\nMEASURED AFTERWARDS, king session, same day 15:03Z.\n\nThe lock file is /Users/bb16/.fno/agents/locks/king-footnote-g4.lock with mtime 2026-08-15T02:14:58Z. That is thirteen hours before the failed sends. The holder that created it was gone long before the worker ever tried.\n\nEvery lock in that directory is empty. 0 of 2590 files carry a single byte. So a lock records no pid, no holder handle, no acquisition timestamp, and no ttl. Nothing can distinguish a live holder from a corpse, which is why this one blocked for thirteen hours and why the directory has accumulated 2590 of them with no reaper. 0 have been touched since 14:00 today.\n\nTHE LOSS IS TOTAL, NOT DEFERRED. Grepping the bus at ~/.fno/bus/messages.jsonl for b2bf, for the worker session id 306cde5a, and for the doc subject returns 0 hits across all 504 lines. The message was never written durable. The lock timeout aborted the send instead of falling back.\n\nThat breaks the receipt contract the project already relies on. The standing rule is that delivered (hosted) is confirmed and queued (durable) may never drain. This path produces neither. There is no receipt at all, so a sender that trusts the vocabulary has no way to learn the message is gone. The only reason the king ever saw this content is that the operator copied the worker pane into a chat window by hand.\n\nCONTRAST WITH THE INJECT PATH, which is the useful diagnostic. Two other blueprint completions did reach the bus that morning, msg-845bbb at 07:15Z and msg-7244d3 at 07:43Z, and both sat unread. So a durable write can succeed while the inject wake fails. Here even the durable write did not happen. Those are two different defects and this node owns the second.\n\nFIX SHAPE. Write the holder into the lock: pid, handle, acquired_at, and a ttl. Judge staleness from that content and break a lock whose holder is provably gone, which is the only thing that would have unblocked a thirteen hour old file. Make a lock timeout fall through to the durable bus and exit non-zero, never abort the send. Reap the directory, because 2590 empty files is the accumulated evidence that nothing ever has.\n\nVERIFY. A test where the lock is held by a dead pid and the send still lands durable. A test that the send exits non-zero when it could not inject, so a caller can tell the difference. A test that asserts the bus grew, which is the positive marker; asserting the absence of an error would pass on a send that wrote nothing.\n\n\nTHIRD SPECIMEN, king session, 2026-08-15 15:05Z onwards, and it widens the defect from the lock to the send verb itself.\n\nAttempting to answer t-xb2bf reproduced the loss from the sending side twice, in two different shapes.\n\nThe first shape is an unaddressable live row. fno mail send t-xb2bf resolved the registry row, printed six damaged session alias warnings, printed no receipt, and wrote nothing. That row is status live with mail_handle null. So a live recipient with no handle accepts a send and silently drops it.\n\nThe second shape is a hang. Every subsequent send, addressed by short id and including one to 1c122e12 that had succeeded 70 minutes earlier at 13:51Z, returns EXIT=124 under timeout with empty stdout and empty stderr. The bus stayed at 504 lines across all attempts. The verb neither delivers nor queues nor reports.\n\nRULED OUT. The king lock had been removed minutes before, so it was the obvious suspect. Recreating the file did not restore sends, and the failure reproduces with the lock present and absent. The deletion is not the cause.\n\nTHE ALIAS CORRUPTION IS VISIBLE AND HAS A SHAPE. Every warning doubles the short id: fno-dbc28468-dbc28468, etl-6e6b209f-6e6b209f, web-e50311e7-e50311e7. The alias is being built as project-shortid-shortid. dbc28468 is a worker spawned four minutes before the warning fired, so fresh rows are written corrupt at registration time. That is why regenerating from the default never sticks. The regeneration is correct and the writer re-corrupts it on the next spawn.\n\nWHAT THIS COSTS. With send wedged, the king can receive durable mail but cannot answer it. Coordination degrades to the operator copying worker panes into a chat window by hand, which is exactly how the x-b2bf design doc arrived.",
    "domain": "code",
    "id": "x-b281",
    "slug": "agent-mail-lock-never-expires-carries",
    "status": "in_review",
    "title": "An agent-lock timeout in mail send loses the message instead of queueing it",
    "type": "feature"
  }
]"""


def _fixture() -> list[dict]:
    return json.loads(_FIXTURE_JSON)


def _by_id(rows: list[dict]) -> dict[str, dict]:
    return {r["id"]: r for r in rows}


FAMILY = {"x-17ba", "x-49c8", "x-5756"}
TRUE_PAIR_SCORE = 0.2636  # x-17ba <-> x-49c8, the family's weakest true edge


def test_family_probe_finds_both_siblings_at_reading_floor():
    # The scorer's recall is the plan's headline measurement: both other
    # members of the real family land in the top 5 (measured ranks 1 and 2)
    # once the floor drops from the dedup 0.30 to the reading 0.15. x-5756
    # RANKS FIRST, above x-49c8, only while details are in the token surface:
    # drop details and the order flips (x-49c8 first), which is this fixture's
    # observable slice of the full-graph measurement where the rank fell to 31.
    top = similar_nodes(_by_id(_fixture())["x-17ba"], _fixture(), k=5, floor=_MIN_SCORE)
    ranked = [node_id for node_id, _, _ in top]
    assert {"x-49c8", "x-5756"} <= set(ranked)
    assert ranked[0] == "x-5756"


def test_default_floor_still_truncates_the_family_to_empty():
    # Pins the exact truncation being fixed: the same probe, same fixture,
    # default floor (intake's tuned 0.30 from x-6ac7) returns []. If someone
    # lowers _DEDUP_MIN_SCORE this fails and forces a re-read of the evidence
    # that 0.20-0.27 is where noise lives.
    assert similar_nodes(_by_id(_fixture())["x-17ba"], _fixture(), k=5) == []
    assert _DEDUP_MIN_SCORE == 0.30  # the pinned constant the above depends on


def test_noise_pairs_stay_below_reading_band_and_out_of_family_probe():
    rows = _by_id(_fixture())
    # maintain's rollup leg proposed all four of these as children of the
    # x-b917 epic, every score 0.20-0.21: prose-Jaccard noise, not relatedness.
    for noise_id in ("x-3bd3", "x-c136", "x-1fbb", "x-b281"):
        score, _ = _score(rows[noise_id], rows["x-b917"], _tokens(rows[noise_id]), _tokens(rows["x-b917"]), include_epic=False)
        assert score < 0.22, f"{noise_id} vs x-b917 rose to {score}"

    # On the live graph (family superseded, filtered) x-3bd3's top 5 is five
    # unrelated nodes and none of the family. In this pre-supersede fixture
    # family members DO appear for the unrelated probe at 0.2215-0.228, below
    # every true family edge - the same numbers as the noise band. That is the
    # arithmetic the gate is built on: the list is a reading aid, not a
    # verdict, so the outcome is a full-context judgment, never the score.
    top = similar_nodes(rows["x-3bd3"], _fixture(), k=5, floor=_MIN_SCORE)
    assert all(score < TRUE_PAIR_SCORE for _, score, _ in top)
    assert "x-17ba" not in {node_id for node_id, _, _ in top}
