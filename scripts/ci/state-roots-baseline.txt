# state-roots-baseline.txt - the ratchet for `fno doctor lint state-roots`.
#
# Epic x-3d21 R4 rules that every fno state access resolves against a DECLARED
# root. Eighty-four sites already build one by hand. Migrating them is not
# this gate's job (the Rust journal sites are x-1571's, the duplication half is
# x-6f9f's), so they are held here with an owner and a reason while the gate
# stops the next one appearing.
#
# FORMAT, tab-separated:  RULE<TAB>path<TAB>key<TAB>owner<TAB>reason
#   RULE   A = a hand-built `<root>/.fno/<state file>` path.
#          B = a zero-argument lru_cache/cache over a reader that resolves a
#              state root (x-3d21 R5).
#   key    rule A: the state filename. rule B: the function name.
#   owner  the node that owns draining it, or `unowned`.
#
# The key is (RULE, path, key) and deliberately carries NO line number: a line
# number moves on every unrelated edit above it, so a line-keyed baseline
# churns, and churn teaches people to regenerate the file, which is how a
# ratchet stops ratcheting. The stated cost is that two hand-built paths for
# the same state file in one module drain as one entry.
#
# THE GATE FAILS IN BOTH DIRECTIONS, and the second is the load-bearing half:
# a hit not listed here fails, AND a line listed here whose site no longer
# matches fails with "fixed, remove this line". Without the second direction a
# drained line becomes a permanent exemption.
#
# There is no --update flag on purpose. Adding a line is a deliberate act with
# an owner attached, in a diff a reviewer reads.

A	cli/src/fno/agent/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/agents/context.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/agents/spawn_gate.py	claims	unowned	call the resolver STATE_FILES names for this file.
A	cli/src/fno/agents/truth_status.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/agents/whoami.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/annotate/core.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/backlog/advance.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/backlog/batch.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/backlog/capture.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/backlog/capture.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/claims/incarnation.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/claims/self_identity.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/cli.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/config_cli.py	ledger.json	unowned	call the resolver STATE_FILES names for this file.
A	cli/src/fno/config_cli.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/cost/_register.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/cost/_register.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/delivery/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/events/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/events/cli.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/events/gate_escape.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/events/log.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/graph/_reconcile.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/graph/_reconcile.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/graph/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/graph/node_builder.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain. The site moved here verbatim from graph/cli.py with the node builder.
A	cli/src/fno/graph/triage.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/king/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/mail/cli.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/observer/cli.py	ledger.json	unowned	call the resolver STATE_FILES names for this file.
A	cli/src/fno/observer/isolation.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/observer/isolation.py	ledger.json	unowned	call the resolver STATE_FILES names for this file.
A	cli/src/fno/observer/isolation.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/outstanding/cli.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/phase/cli.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/post_merge_route.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/pr/_attestation.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/pr/_review_hold.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/pr_watch/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/provenance/spawn_think.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/provenance/spawn_think.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/recovery.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/research/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/resume/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/retro/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/scoreboard/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/agent/cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/state/outage_handoff.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain. The site restores a worktree-scoped manifest during a handoff, so it resolves from its worktree parameter, not the process cwd.
A	cli/src/fno/state/v2_paths.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/target_cli.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/target_cli.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/update.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/verify_advise.py	events.jsonl	unowned	Python journal site surviving PR 1252; fix is fno.paths.project_events_json().
A	cli/src/fno/wake/detect.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/worker/cli.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	cli/src/fno/worktree_stranded.py	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	crates/fno-agents/src/king_board.rs	events.jsonl	x-1571	the board port's project-journal anchor: the needs fold reads the same journals the needs verb reads, and no Rust project-events resolver exists yet; x-1571 owns draining Rust journal sites.
A	crates/fno-agents/src/active_backlog.rs	events.jsonl	x-1571	surviving Rust ProjectJournalPath site; x-1571 owns draining these and carries a standing warning against re-adding the events pin here.
A	crates/fno-agents/src/claims.rs	events.jsonl	x-1571	surviving Rust ProjectJournalPath site; x-1571 owns draining these and carries a standing warning against re-adding the events pin here.
A	crates/fno-agents/src/daemon.rs	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	crates/fno-agents/src/finalize.rs	events.jsonl	x-1571	surviving Rust ProjectJournalPath site; x-1571 owns draining these and carries a standing warning against re-adding the events pin here.
A	crates/fno-agents/src/kill_criteria.rs	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	crates/fno-agents/src/loop_runtime.rs	events.jsonl	x-77f7	canonical Rust ProjectJournalPath constructor; x-77f7 owns the shared caller-root path seam.
A	crates/fno-agents/src/loop_target.rs	events.jsonl	x-1571	surviving Rust ProjectJournalPath site; x-1571 owns draining these and carries a standing warning against re-adding the events pin here.
A	crates/fno-agents/src/loop_target.rs	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	crates/fno-agents/src/loopcheck.rs	events.jsonl	x-1571	surviving Rust ProjectJournalPath site; x-1571 owns draining these and carries a standing warning against re-adding the events pin here.
A	crates/fno-agents/src/loopcheck/authorship.rs	target-state.md	x-3a6f	the manifest author read moved here from loopcheck.rs with the authorship wiring; the Rust side cannot call the Python resolver (fno.paths.target_state_path), so the hand-built fallback stays a declared root until a Rust resolver exists.
A	crates/fno-agents/src/gc_sweep.rs	ledger.json	unowned	best-effort ledger enrichment for the reap receipt (moved from daemon.rs with the sweep); the receipt stands on the row when the path is overridden.
A	crates/fno-agents/src/graph_get.rs	graph.json	unowned	same class as crates/fno/src/backlog_view.rs below; no Rust resolver exists yet for graph.json.
A	crates/fno-agents/src/manifest_lookup.rs	target-state.md	unowned	target-state.md has NO resolver today (STATE_FILES records resolver=None). That gap is the finding; a resolver must exist before this can drain.
A	crates/fno-agents/src/state_path.rs	target-state.md	unowned	this module IS the hook-facing resolver; the hand-built line is the one-release legacy checkout fallback and drains when it retires.
A	crates/fno-agents/src/paths.rs	ledger.json	unowned	paths.rs IS the resolver module; the hand-built line is the ledger_path legacy checkout migration anchor and drains when it retires.
A	crates/fno-agents/src/spawn_gate.rs	claims	unowned	call the resolver STATE_FILES names for this file.
A	crates/fno/src/agents_view.rs	claims	unowned	call the resolver STATE_FILES names for this file.
A	crates/fno/src/agents_view.rs	registry.json	unowned	call the resolver STATE_FILES names for this file.
A	crates/fno/src/backlog_view.rs	graph.json	unowned	call the resolver STATE_FILES names for this file.
A	crates/fno/src/mux_cli.rs	events.jsonl	x-1571	surviving Rust ProjectJournalPath site; x-1571 owns draining these and carries a standing warning against re-adding the events pin here.
