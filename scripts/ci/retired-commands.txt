# Commands a ruling has retired. A caller-facing string that shows one of
# these in RUNNABLE form fails scripts/ci/check-retired-command-strings.sh
# unless it carries a `retired-ok:` marker saying it describes rather than
# instructs.
#
# Format: <retired command>|<what to say instead>|<ruling id>
#
# Adding a line here is how a ruling reaches the moment of acting. Recording
# it in the decision record alone does not: the decision record fires when
# someone goes looking, and a runtime string fires when someone is about to
# act. Only the second is the moment that matters.
claude rm|fno agents rm, then fno agents adopt <session-id> to reverse it|d-1900e419
worktree cargo-offload|nothing: cargo writes outside the checkout now (build.build-dir); reclaim stale intermediates with fno agents workspace worktree cleanup --cargo-targets|the cargo build-dir move
config setup codex-plugin|fno config plugin install codex [--force], the one install verb for every harness|the one-install-verb ruling
claude stop|fno agents stop|d-1900e419
# root folds: the old root spellings forward for one release, but no
# caller-facing string may TEACH them. Full two-token paths so the canonical
# nested spellings (fno backlog done, fno inbox decide, ...) never match.
fno done|fno backlog done (same flags)|d-9de9f05b
fno decide|fno inbox decide|d-add90c60
fno law|fno inbox law|d-add90c60
fno mail|fno agents mail (the form every skill already teaches)|d-b93d7754
fno test|fno doctor test|d-df6c29a6
fno workspace|fno agents workspace|d-cf2d6fe1
fno project|fno config project|d-344fe242
fno yard|fno agents yard|the root-verb fold
fno runtime|fno agents workspace (retired outright, tombstoned)|d-cf2d6fe1
--model-tier|fno backlog update --difficulty <band> - the work-difficulty axis (low, medium, high)|the difficulty-axis rename
# The launch-on-write hook stays unrunnable. Blueprint completion dispatches nothing; a king or the drain runs fno backlog advance.
autolaunch-on-ready.sh|fno backlog advance (the ordered drain start)|the one-drain-start ruling
# d-e1eec854 collapses /fno:law to one user-invoked step. The staged-proposal
# ceremony is gone: one `fno inbox law set` call records and returns a d- id.
inbox law prepare|fno inbox law set (one call, no staged proposal)|d-e1eec854
inbox law enact|fno inbox law set (one call, no hash and no receipt)|d-e1eec854
inbox law resume|fno inbox law set (nothing is staged, so nothing resumes)|d-e1eec854
inbox law inspect|fno inbox decisions <subject> --lane law|d-e1eec854
# d-496680aa retires the second launch verb: one launch verb, and it
# is spawn. The door renders the verb, brief and lane for a node-seeded spawn.
fno agents dispatch|fno agents spawn --node <id>|d-496680aa
fno dispatch|fno agents spawn --node <id>|d-496680aa
# d-fe66560a: hidden verb roots are not allowed at all. The loaded-thread
# answer rides fno-agents list --json --harness codex as the codex_loaded
# block; a caller-facing string may never teach the bare verb.
codex-loaded-threads|fno-agents list --json --no-discovered --harness codex (the codex_loaded block)|d-fe66560a

# The one king skill: the one-wave pass is reign --once.
king-for-a-day|/fno:reign --once <scope>|the one-king-skill fold

# Never shipped: step 2a-bis of the blueprint skill named it for the question door.
fno inbox ask|fno inbox outstanding ask "<question>" --node <id>|never shipped: the question door is fno inbox outstanding ask

# x-26bd: the annotate verb retired; note --blocking / notes findings / note
# --resolve are the only spellings that touch the findings gate.
backlog annotate add|fno backlog note <node> "<text>" --blocking|x-26bd one finding store
backlog annotate list|fno backlog notes findings [<node>]|x-26bd one finding store
backlog annotate resolve|fno backlog note --resolve <finding-id>|x-26bd one finding store
fno backlog annotate add|fno backlog note <node> "<text>" --blocking|x-26bd one finding store
fno backlog annotate list|fno backlog notes findings [<node>]|x-26bd one finding store
fno backlog annotate resolve|fno backlog note --resolve <finding-id>|x-26bd one finding store
fno mux block annotate|fno mux block annotate still shells note --blocking (internal)|x-26bd one finding store
