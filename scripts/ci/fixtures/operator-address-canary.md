Ask the operator for approval. CANARY-HIT

The `operator` authority value is literal.

```text
The operator inside a fenced example is literal.
```

The operator ruling was recorded.

The operator override was not allowed.
