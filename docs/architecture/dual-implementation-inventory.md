# Dual-implementation inventory and the port protocol

The operator ruling. When any trigger fires, port a Python behavior to Rust.

1. DUPLICATION. One behavior is hand-written twice, in two languages. This is the original rule, and the dual-logic kind below.
2. PREVENTION. The port removes a class of defect that a type system makes unrepresentable rather than merely detectable. The qualifying classes are in the second-trigger section below.
3. PARITY GUARD. Writing a guard whose purpose is to force Python and Rust implementations to agree is itself the strongest trigger for a port. The guard is legitimate as scaffolding during the port, never as its end state.

The first two triggers do not reach a shared-vocabulary or a generated-artifact guard. Those stay excluded regardless of benefit, because porting them retires nothing and a generated-artifact tripwire is correct as built. The parity-guard trigger applies only when the guard pins two implementations of one decision, not when it checks shared vocabulary or generated data.

This page answers four questions. Which cross-tree guards mark a real second implementation. What the procedure is for retiring one. Why most of these guards are not port candidates at all. When a port is justified without duplication.

## The discriminator

A parity guard exists because two things can drift. Two implementations do not follow from that.

There are three kinds. Only the first is a port candidate.

**dual-logic.** Two hand-written implementations of one behavior, in two languages. A port deletes one leg. The guard then converts to a characterization test. This is the only kind the operator ruling is about.

**shared-vocabulary.** One concept declared, spelled, or stamped at many sites. The guard checks that the sites agree. Porting one site does not retire the guard. The concept still has to be spelled the same way everywhere it appears. Several sites are not code at all. A workflow YAML, a ruleset data file, and a setup-doc filename are all such sites. When a guard's surfaces include a non-code artifact, no port can ever retire it.

**generated-artifact.** One owner, a generated copy, and a freshness tripwire. This is correct and must not be "fixed". The exemplar is `harness_capabilities.toml`. The Rust tree owns the canonical file. `build.rs` generates the Python copy and the mux-crate copy on every build. The "Generated copies match their sources" step in `cli-ci.yml` catches a stale copy. One owner, one generated artifact, one guard. A generated data file is not a second implementation. Every generated copy has a row in `generated-artifacts.tsv` or `skill-bundles.yaml`, and `scripts/ci/check-generated-manifest.sh` enforces this.

This page exists to prevent one failure: reading a filename as evidence. A file called `*_parity.rs` looks like a live second implementation. Often it is not. The inventory below was filed wrong twice for that reason. The guard in the last section makes the distinction machine-readable.

## The second trigger: prevention

Duplication is not the only reason a port pays. The second trigger is prevention: the port removes a class of defect that a type system makes unrepresentable rather than merely detectable. Detection-only fixes leave the wrong answer still writable. A type that cannot express the wrong answer ends the class. Every case below is one measured incident of the same failure class: an instrument returning a value indistinguishable from a real answer.

The provenance case. Two docstrings stated opposite meanings for one registry lookup: one said a hit proves the id is NOT ours, the other that it IS ours. Both were confident and both were load-bearing. They reconciled only on the provenance of the input, which nothing encoded. Three workers were refused their own node before anyone read both docstrings.

The exhaustiveness case. A capability table recorded agy and opencode as unsupported with no justification, while pi's row carried a measured reason. One word was doing two jobs, measured-absent and never-checked, and no reader can tell them apart.

The rename-safety case. The substrate vocabulary moved from `bg` to `thread`, and a literal comparison against `bg` survived the rename. Every autonomous dispatch then reported it went headless while it spawned a thread, and emitted a false fallback event with it.

Each case earns the port through the type system. Provenance moves into a type instead of a docstring. A closed vocabulary forces every entry to carry its reason. An enum turns a rename into a compile error at every surviving site instead of a false comparison at one.

The scope guard. Prevention is a trigger for porting, not a third kind of implementation. The parity-guard trigger is stronger still: once a guard is needed to force two decision legs to agree, the port has already earned priority. The discriminator above still decides what a dual implementation is, and its exclusions survive these triggers unchanged. Every port follows the same four-step protocol. The sequence section orders confirmed duals by dependency, while a prevention-only case is scheduled on its own measured evidence.

## The parity tests

Measured by reading each file's header, then testing whether its oracle still exists on disk.

| Parity file | Oracle | On disk | Verdict |
|---|---|---|---|
| `crates/fno-agents/tests/claude_ask_parity.rs` | `fno.agents.harnesses.claude._build_envelope` | absent (symbol) | characterization, port complete |
| `crates/fno-agents/tests/codex_ask_parity.rs` | `fno.agents.harnesses.codex.resume` | absent (symbol) | characterization, port complete |
| `crates/fno-agents/tests/graph_store_parity.rs` | `fno.graph.store._acquire_flock` | absent (symbol) | characterization, port complete |
| `crates/fno-agents/tests/kill_criteria_parity.rs` | `scripts/lib/kill-criteria.sh` | absent | characterization, port complete |
| `crates/fno-agents/tests/verify_evidence_parity.rs` | `scripts/lib/verify-event-evidence.sh` | absent | characterization, port complete |
| `crates/fno-agents/tests/claim_classifier_parity.rs` | `fno.claims.staleness` | absent (module) | characterization, port complete |
| `crates/fno-agents/tests/backlog_ready_parity.rs` | `fno.backlog.explain.build_selection_filters` | absent (symbol) | characterization, port complete (2026-09-08) |
| `crates/fno-agents/tests/fleet_task_reconcile_parity.rs` | `fno.agents.stale_escalate.answered_question` | absent (symbol) | characterization, port complete (2026-09-22) |

All eight are finished ports. Their oracle legs are gone. Each converted case now asserts against a golden captured before that deletion, and each file sits at step 4 of the protocol below. None awaits anything. The ask files, the graph store file, and the ready-selection file carry the SYMBOL oracles, and their Python modules survive the port. The claim classifier's module oracle is absent because its Python module was deleted. The spawn surface keeps `parse_short_id`, the session registry keeps the reply readers, and `codex.create` keeps the one-shot lane. Only a symbol or module resolution says which leg is gone.

`kill_criteria` is not a dual implementation. State the reason, because the mistake is easy to repeat. The Python files under `cli/src/fno/plan/` validate the kill-criteria DECLARATION against a schema. The Rust `kill-check` verb EVALUATES the predicates at a wave boundary. Two functions share a vocabulary. The parity test never pointed at that Python. It points at a bash oracle that is gone.

## The real dual set

**The restart verb's mux leg.** Dual, disposition dual-logic, RETIRED by the session-survival PR (2026-09-17). The Python leg was `cli/src/fno/restart.py` `restart_command`, which captured and dropped kill-server's output. The same module also held the mux session policy: the partition, the spare and wedged rows, and the break-glass lever. The Rust owner is `crates/fno-agents/src/restart_run.rs` `run_restart` plus `crates/fno/src/mux_cli/kill_policy.rs`, the pure row partition the kill-selector and the restart verb share. The Python module is now a pass-through that execs `fno-agents restart` and returns its exit code. The partition's behaviours are asserted by Rust tests beside the code.

Fourteen live duals below. Retirements are recorded where they landed: the watchdog reap and retire lanes (2026-09-04), the node-dispatch launchers (2026-09-12), and the spawn gate (2026-09-13). The ask adapters, the claim classifier and the graph store are retired (2026-09-02), the review-freshness mirror followed (2026-09-03), and the ready-selection dual is retired (2026-09-08). The reign-verdict inputs dual was added and retired in one PR (2026-09-15). The native graph reader remains the confirmed dual closest to a port, with fourteen port-owed duals named below.

**The spawn gate.** Dual, disposition dual-logic, RETIRED 2026-09-13 by the one-gate collapse. The legs were `cli/src/fno/agents/spawn_gate.py` `run_gate` and `crates/fno-agents/src/spawn_gate.rs` `run_gate`. The Python leg covered pane, `--route`, account and wake revival. The Rust leg covered the unrouted bg/headless arms from `bin/client.rs`. SIX Python re-readers recomputed pieces of the same answer: `probe_capacity`, `explain.gates_for`, `top.lane_rows`, `route_resolve._vendor_tables`, advance's dispatch width, and doctor_lanes' share reading. The Rust gate was a strict subset. It had no provider cap, no account quota lock, no king share, and no registry schema check. Any new conjunct had to be threaded through eight Python files.

The port made the Rust gate the ONE gate. It owns every axis, in the exact order the Python gate evaluated them: fleet incident, schema, quota lock, provider cap, CPU, slots, RAM, king share. The `spawn-gate` verb answers `gate` and `probe` modes over one JSON round trip. The Python `run_gate` kept its signature and became the transport, so the twenty-plus test files that monkeypatch it stayed valid. `probe_capacity` is the verb's probe mode. Deleted with the Python leg: the provider counter (`provider_live_count` and its two helpers), `share_reading`, `_king_share`, the refusal helpers, and their tests. `census` survives for the display surfaces: `fno agents top`, orphans, and unfinished work.

**The reconcile fold's dedup legs.** Dual, disposition dual-logic, RETIRED 2026-09-22 by the fleet-task channel. The legs: the Python reconcile fold in `cli/src/fno/agents/stale_escalate.py`, its inline fold plus `answered_question`, `reset_answered`, `_MECHANICAL_CLOSERS`, `_open_questions`, `_close_question` and `_try_close_supersede`. Two Rust copies of the "already asked?" read joined them, `open_questions_mention` in `heal.rs` and in `pr_nudge.rs`. `crates/fno-agents/src/fleet_task.rs` (`file_once`, `reconcile`, `close`) is the one surviving owner. The Python body is one `verb_call("fleet-task", ...)` transport. `already_asked` survives for the writers that stay questions. The characterization is `crates/fno-agents/tests/fleet_task_reconcile_parity.rs`, goldens captured from the Python fold before the deletion.

Surviving Python names, with callers, so nothing is deleted quietly. `census`/`LiveCensus` serves top, orphans and unfinished work. `_pid_alive` and `_roster_path` serve census. `available_ram_gb` serves census callers. `_cpu_axis`, `_prefetch_fleet_reading`, `_load_cpus` and `_load_snapshot` serve doctor_lanes' advisor arm and the footprint tests. `provider_lanes_cap` serves scoreboard/fold and advance. `GateGuard` and `consume_provider_admission` serve the spawn seam and the wake revival's `retain_revived_worker`. `_refuse` and `_emit_gate_event` are the one refusal-emitting seam. The QoS helpers serve the pane dispatch. The axis tests moved to Rust beside the code, in `spawn_gate.rs`, `spawn_gate_lanes.rs` and `spawn_gate_verb.rs`. The Python suites stub the verb and assert the transport contract and the probe answer. The exit-code table gained one number, 86 `gate_unavailable`, byte-parity in both trees. The merged meanings of 82/83 fleet, 84 state root and 85 sandbox are unchanged.

**The node-dispatch launchers.** Dual, disposition dual-logic, RETIRED (2026-09-12). This pair is Python/Python, a first for the register, and the disposition still decides: two hand-written implementations of one behavior, "launch a worker for a backlog node". Two languages was incidental to the discriminator all along. The legs: `cli/src/fno/dispatch.py` `_dispatch_one`, the mux keystroke's leg, and `cli/src/fno/backlog/advance.py` `_spawn_worker`, which resolved preferences and then shelled `fno agents spawn`. The dispatch leg hardcoded the claude harness and the pane substrate. It also carried an inline copy of the family-2 guard, the spawn gate, and the release choreography. The two legs were caught diverging twice. The account-picking incident (recorded at `autonomous_route.py`) produced one hoisted shared function plus a parity test. The harness answer was worse: the same node dispatched by the keystroke and by `fno backlog advance` landed on different harnesses and models. The port (2026-09-12) makes `agents.node_dispatch.resolve_node_spawn` + `node_spawn_argv` the ONE preference path and `fno agents spawn --node <id>` the ONE launcher. `fno agents dispatch next` (renamed from `one`, hidden alias kept) is selection + the resolver + one shellout. Advance keeps the resolver plus its own shellout. Deleted with the leg: the hardcoded harness, `_cutover_command`, the inline guard/gate/release pair, and the parity test `test_route_tuple_identical_across_launchers` in `cli/tests/unit/test_advance.py`. That test existed only to hold the two legs equal. Keeping a harness that forces two legs to agree is the move working principle 9 names as the port trigger. Surviving callers, so nothing is deleted quietly. `select_autonomous_route` survives for advance's route read. `_next_node` survives as board selection (`fno backlog next`). `_worktree_ensure_for_launch` survives as the launch-cwd read. `_grid_lane_for` survives as the capacity read inside `resolve_node_spawn`. `resolve_dispatch_brief` survives in advance's brief chain and the spawn door's `--node` seed.

**The reign-verdict inputs.** Dual, disposition dual-logic, ADDED AND RETIRED in the same PR (2026-09-15). The record exists to name which Python legs died and which transports survive, per the port protocol's own rule. PR 1911 shipped the `king-verdict` verb with Python owning every input. `cli/src/fno/king/history.py` resolved the caller crown and manifest, loaded king config, enumerated the tracker rows, and computed the three-check-in window. It passed nine precomputed values on the argv. `cli/src/fno/king/scope.py` `_ts_key`/`scope_split` keyed the inherited/filed split. `cli/src/fno/king/cli.py` re-resolved the scope for escalation. The port (2026-09-15) makes `crates/fno-agents/src/king_verdict_inputs.rs` the ONE input owner: crown, canonical scope, manifest, config values, graph scope, window, and delivery split, every refusal typed. Deleted with the legs: `run_native_verdict`, `resolve_manifest`, `verdict_counts`, `_ts_key`, `scope_split`, `handoff_scope`, and the Python question-text renderer. The argv facts died too: `--window`, `--compaction-ceiling`, `--inherited-undelivered`, `--filed-undelivered`, and `--inherited-closed-in-window`. Passing one is now a usage failure. Surviving transports, so nothing is deleted quietly: `verdict_read` and `resolve_scope`/`run_native` in `king/history.py`. Also alive: `verdict_cmd`'s stdout-and-exit passthrough, `escalate_cmd`'s summary threading into the natively rendered question, and the durable channel writes in `king/escalate.py`.

CLOSED (2026-09-14, operator ruling). The pair is now single. The keystroke's leg (`fno agents dispatch next`, selection plus resolver plus one shellout) is deleted. The mux keystroke reads the board itself and shells `fno agents spawn` directly. `dispatch resolve`, `capabilities` and `family` went with it. The two read leaves live on as hidden Rust verbs (`fno agents capabilities`, `fno agents target-family`). The shell dispatcher (`dispatch-node.sh`) passes the node and only what the human typed. One launch verb, and it is spawn.

**The preflight wait queue.** Dual, disposition dual-logic, ADDED AND RETIRED in the same PR (2026-09-20). The pair: the five bash functions in `scripts/ci/preflight.sh` (`enqueue_ticket`, `dequeue_ticket`, `ticket_is_dead`, `am_i_front`, `queue_has_waiters`) and the new `crates/fno-agents/src/claim_queue.rs`. Both answered which waiter acquires a contended machine-wide lock next. The bash leg ordered preflight's own repo lock only. The same defect in `test_run.rs`'s `acquire_claim_blocking` had no order at all. It has three callers: test suite, build:cargo, and run slots. On 2026-09-20 seven `fno-agents test-run` processes contended for `test:suite`, the oldest 27 minutes into its budget. The port makes `claim_queue.rs` the ONE arrival-order owner. Ticket numbers allocate above the highest survivor and never in a dequeued hole. The front `keys.len()` waiters are admitted, which keeps the run-slot counted semaphore from serialising to one. Dead and recycled tickets are reaped as the scan walks. A ticket stamped on another machine is skipped, not ordered. `preflight.sh` deleted the five functions and calls the deployed binary's `claim queue enter|front|leave` verbs. Its wait loop, steal policy, cancellation poll, and status prints are untouched. A concurrency property has no golden capture. The equivalent proof is the property tests in `claim_queue.rs` (lapping, ordering, crash survival, pid recycle, allocation, hole, foreign skip) plus one real preflight run. The PR writes no `*_parity.rs`, and `check-parity-test-provenance.sh` has nothing to assert.

**The ready-selection decision.** Port complete (2026-09-08). The old leg had two spellings. `cmd_ready` inlined eight filters. `cmd_next._pick_ready` ran the same eight through `fno.backlog.explain`'s cascade so `advance --explain` can narrate them. The two agreed only because they shared five helpers by hand. The decision now lives once in `crates/fno-agents/src/backlog_ready.rs`, served over the keeper's `ready` verb. Five callers read it as clients: `cmd_ready`, `cmd_next`, the two advance wrappers (`_ready_nodes`, `_ready_leaf_children`), and the king board's unplanned queue. `advance --explain` renders the reply's drops. The goldens under `tests/golden/backlog_ready/` were captured from the Python leg while both legs ran, and `backlog_ready_parity.rs` characterizes rows, drop attribution, and survivor sets against them. Port-owed rows stay alive with named callers, not silently deleted: `selection_guards`, `_guard_staleness_days`, `is_stale_ready`, `plan_rung`, `dispatch_hold_verdict`, and `make_selection_sort_key`. The `next` observer merge re-verifies its rows through `selection_guards`. The board renderer sorts through `make_selection_sort_key`. `store._plan_rung_map` reads rungs Python-side for the write path. Those Python predicates survive with named callers rather than as a second selection leg.

**The opencode install.** Dual, disposition dual-logic, RETIRED 2026-09-17. The legs were the five Python helpers in `cli/src/fno/setup/integration.py`: `_opencode_plugin_src`, `_opencode_plugins_dir`, `_opencode_plugin_dest`, `_opencode_is_installed`, and `_opencode_install`. They byte-compared one copied file. The second leg, `install_opencode` in `crates/fno-agents/src/plugin_install.rs:470`, copied the same one file. The two had no parity guard. Both shipped a bridge-only install reported as the word installed. The port makes `crates/fno-agents/src/opencode_install.rs` the ONE installer. It writes the stop bridge, generated `fno:<verb>` command files named to match `render_verb_seed` and `opencode_run_tail`, translated agent files, skill trees, and the content-hash manifest. The five Python helpers and the doctor leg's imports of them are deleted. The setup adapter and the doctor surface report read one JSON receipt through the fno-agents door (`plugin-install opencode --status/--installed/--json`). Pending, deliberately NOT in this PR: the bridge asset `cli/src/fno/setup/assets/opencode/footnote.js` moves out of the Python package tree. It moves only after the sibling that owns both plugin bodies lands its V2 arm and stops editing the file.

**The review-freshness mirror.** `_pr_code_diff_identity` and `_reviewed_sha_still_describes_head` in `cli/src/fno/pr/_reviews.py` mirrored `review_freshness` in loopcheck.rs decision for decision. When the Rust instrument was absent, they were the fallback answer. Retired 2026-09-03. The fallback guessed where every other missing-instrument gate refuses. It also disagreed with the gate it mirrored: a locally recomputed floor read 3 where the gate's row said 1 on one PR. The one predicate now lives in `crates/fno-agents/src/review_freshness.rs`. With the binary absent, `fno do pr status` answers `unmeasurable` and names the remedy.

**The claim classifier.** Port complete (2026-09-02). The Python liveness module was deleted, every verdict consumer moved to the Rust decision door, and `claim_classifier_parity.rs` now characterizes the captured state+basis golden. Its parity guard is the third trigger's specimen: writing the guard proved that the duplicated decision had to be retired.

**The ask adapters.** `fno.agents.harnesses.claude` and `fno.agents.harnesses.codex`, pinned by the two differential harnesses until their port completed (2026-09-02). The Rust runtime already owned production `ask` end to end. The port deleted the Python ask functions and moved the last Python-only caller duty, `--to-project` anycast resolution, into `cmd_ask` ahead of the binary exec. The surviving Python surface is spawn substrate, not ask: `parse_short_id` for bg receipts, the registry reply readers, and `codex.create` for one-shot spawns.

**node_seed.rs** is NOT a dual. It takes the lifecycle table's answer as projected input, so the ported table stays the one table. The lifecycle verb table and the doc-kind classifier live once in `backlog_ready.rs` (`effective_verb` / `is_blueprint_doc`), served over the keeper's `effective_verb` verb. The Python leg (`harness_map.resolve_effective_verb` and its constant maps) is deleted. `node_dispatch` is a keeper client (`store.request_effective_verb`) and carries no decision of its own. It takes the seed slot from `_seed_slot`, so the argv scan is not copied either. The Rust side decides pass/profile/compose/refuse over facts Python already owns.

**The seed-node derivation and its receipt.** Dual candidate, retired in the PR that opened it (2026-09-20). The node the seed names could be read in two places: the Rust `node_seed` decision (which already parses the seed's verb token) and the daemon mint (which could re-parse the seed from the spawn request). Both reading the seed would have made the derivation a Rust/Rust dual with no shared code. The disposition: the derivation answers once in `node_seed.rs` (now also over a nodeless payload, `derive`/`pass` with a `derive_reason`), and the Python seam applies the answer by inserting `--node`, so both spawn lanes see an explicit flag and no downstream code re-derives. The one thing the daemon still reads is `node_reason`, a forwarded FACT from the seam's own graph lookup, in the same class as every other request-carried row field - not a decision. The row was written here the day the seam landed and retired by the same edit; the inventory entry is the record, not a live risk.

**The graph store.** `cli/src/fno/graph/store.py`, pinned by the differential harness until its port completed (2026-09-02). The store core lives in `crates/fno-agents/src/graph_store.rs` behind a keeper process. The core owns the byte-compatible JSON I/O, defaults, status recompute, canonicalization, slugs, the bounded lock, and the publish with its backup and sidecar. The keeper model is one process per graph, with the socket beside the file. The Python file survives as the socket client with unchanged signatures. The verb surface above it never moved. The port also removed two defect classes. Typed presence makes an empty overwrite of `details` unrepresentable at the store boundary. The bounded lock answers inside its deadline and never blocks forever.

**The native graph reader.** `crates/fno/src/backlog_view.rs`, 2140 lines of Rust parsing `graph.json` with no subprocess. Its docstrings name the Python oracles it mirrors. Two mirrors still hold on both sides: `KANBAN_COLUMNS` (`cli/src/fno/graph/render.py:26`, mirrored at `backlog_view.rs:380`) and `_kanban_column` (`render.py:71`, mirrored near `backlog_view.rs:390`). The readiness mirror lost its Python counterpart. The store port moved the overlay into `graph_store.rs`, and the Python statuses module now answers through the keeper. This file is not a duplicate that waits for deletion. It is the read-only reader of the mux, and the store client borrows its `kanban_column` for peer lanes. A port here deletes a reader, not a second implementation of the store. Dual and UNGUARDED. The parity is a comment asking a human to remember, and the tests beside it run one side only. The earlier guard enumeration did not find this file. That is the limit the duplicate-discovery sweep exists to close. A mirror that declares itself in a docstring is invisible to a guard census keyed on parity-test files. It holds exactly one seam crossing, so a crossing count alone scores writing more of it as an improvement. The two-axis budget in [the seam doc](rust-python-seam.md) exists for exactly this shape.

**The watchdog reap and retire lanes.** Dual, disposition dual-logic, RETIRED (2026-09-04). The Python lanes (`reap_decision`, `retire_decision` and their apply halves in `cli/src/fno/agents/watchdog.py`) and the Rust sweep answered the same question, which row may be removed, with different keys: the Python lane read a node's `status` plus claim occupancy; the Rust sweep read exit stamps. Both were wrong keys. The retirement question now has one leg, the daemon sweep in `crates/fno-agents/src/gc.rs` + `gc_sweep.rs`, keyed by the reverse join through `node.sessions[]` plus transcript quiet. The characterization is the AC7 test family in `crates/fno-agents/src/daemon/tests/gc_receipts.rs`. The watchdog keeps wake, reroute, ghost and report.

**The two-sweep keeps.** Dual, disposition dual-logic, RETIRED (2026-09-15). Rust/Rust, like the node-dispatch launchers: two languages was never the discriminator. The first leg was the open-PR keep. The roster sweep hand-spelled it in its scope-all arm, and the registry sweep built `open_pr` beside it. The second was the no-provenance keep, unconditional in both sweeps. Both spelled "does this row's keep still hold" from the graph record alone, so the two sweeps can disagree about one PR. The decision now lives once in `open_pr_verdict`, beside `provenance_verdict` in `crates/fno-agents/src/gc_sweep.rs`. The roster sweep calls it with no reader and holds candidates exactly as before. The registry sweep's quiet candidates read the PR itself. The no-provenance leg collapsed the same way. `gc_decide` gates the grace arm on `turn_ended`, the roster sweep answers with its `done` state, and the hold carries a clock either way. No parity guard was written. The deletion is the characterization.

**`claims_root_for`.** Dual, disposition dual-logic, port owed, not in this PR. Python leg `cli/src/fno/claims/io.py` (`claims_root_for`, the canonical prefix list `_GLOBAL_ID_PREFIXES`), Rust leg `crates/fno-agents/src/claims.rs` (`claims_root_for`, the hand-copied `GLOBAL_ID_PREFIXES`). Both answer which claims root a claim key coordinates on. The answer is a routing decision, not shared vocabulary. A prefix in one list and not the other routes the writer and reader to different files. That divergence shipped. The spawn-gate mutex key was colon-less, so both sides answered `None` and every CLI verb read the wrong file. And `groom`/`update` sat in the Python list alone, so the Rust verbs read `<space>/claims` where Python read the global root. The parity guard `cli/tests/unit/test_claims_root_routing.py` holds the lists equal today. Writing that guard is itself the port trigger: a port resolves the key's root where the claim is minted and deletes one list.

**`classify_progress` vs the daemon's list projection.** Dual, disposition dual-logic, port owed, not in this PR. Python leg `classify_progress` in `cli/src/fno/agents/reachability.py:217`, Rust leg the progress fold in `crates/fno-agents/src/daemon.rs` (the list projection, `rendered_status_from_truth` around line 5954). Both classify a session's tail into the progress axis the roster renders, and both are correct today. A port deletes the Python leg when the roster's progress axis answers through the daemon.

**The open-PR binding readers.** Dual, disposition dual-logic, port owed, not in this PR. Python leg `classify_open_pr_bindings` in `cli/src/fno/graph/_reconcile.py` (behind the reconcile heal and `fno do pr list`), Rust leg `classify_pr_bindings` in `crates/fno-agents/src/king_board/prs.rs` (the board's pr_nodes queue feed). Both answer which backlog node an open PR belongs to. The keys run in order: the branch name, the node's `(pr_number, pr_url)` back-pointer, and the body's exact `Backlog-Closure:` trailer. Both are correct today. A second, smaller dual rides inside the pair: the trailer parser itself, `parse_closure_trailer` in `cli/src/fno/pr/closure.py` against `trailer_node_ids` in `prs.rs`. The parser survives a classifier port. The merge-time binder and the PR writer keep reading `closure.py`. The parser needs its own row in that later inventory. A port deletes the Python classifier leg once the reconcile heal and `fno do pr list` read the binding through `fno-agents`.

**`pending_supersession_reason`.** Dual, disposition dual-logic, port owed, not in this PR. Python leg `cli/src/fno/graph/statuses.py:220`, Rust leg `crates/fno-agents/src/graph_store.rs:770`. Both answer whether a supersession lacks merged-PR proof, and both are correct today. The blocked_by edge settlement landed beside them and recorded this row. A port deletes the Python leg and converts the guard to characterization. The readiness predicate it sits beside is already Rust-only.

**`node_is_open`.** Dual, disposition dual-logic, port owed, not in this PR. Python leg `cli/src/fno/graph/_reconcile.py` (`node_is_open`, the selection fan-out's open count), Rust leg `crates/fno-agents/src/graph_store.rs` (`is_open_entry`, the settlement's row filter). Both key off the underlying fields, never the derived status, so both hold on rows that never saw a recompute, and both are correct today. A port deletes the Python leg once the fan-out answers through the store.

**`make_selection_sort_key`.** Dual, disposition dual-logic, port owed, retired on the SELECTION side only. Python leg `cli/src/fno/graph/_intake.py` (`make_selection_sort_key`), Rust leg `backlog_ready.rs`'s sort key. The selection surface converged in this port: `next`, `ready`, lane-fill, and the epic fan-out all order through the native leg. The Python key SURVIVES with named callers: the board renderer (`render.py`), which sorts cards for display with `swimlane=True`, and the undispatched observer's ordering. A port here deletes a renderer's sort, not a second selector. It is recorded rather than deleted quietly, per the plan's surviving-caller rule.

**The receipt handle readers.** Dual, disposition dual-logic, port owed, not in this PR. Rust leg `crates/fno-agents/src/resume_receipt.rs:37-41`, Python leg `cli/src/fno/agents/history.py` (`_receipt_answers`). Both answer "which reap receipt does this handle name", and after 2026-09-08 both hold the same three-key rule: `harness_session_id`, `short_id`, `row_name`, compared case-insensitively. Before that the Python leg matched one key of the three, so `fno agents history t-d145-agy-thread-glm` reported no receipt while `fno agents resume` found it. The port direction is Python history into the Rust reader, once history's other two legs (live registry, ledger) have a native door to read through.

No parity guard is being written for it, deliberately. Writing one is the trigger to port instead. A guard here buys the second leg tenure it has not earned. The row is the record until the port lands.

**The registry removal-receipt writers.** Dual, disposition shared-vocabulary, port owed, not in this PR. Rust leg `stage_removal_accounting` in `crates/fno-agents/src/receipt.rs`, Python leg `_stage_removal_receipt` in `cli/src/fno/agents/registry.py`. Both write the same `reap-receipts/<harness>-<session>.json` shape through the `update_registry` choke point. A Python-written removal receipt and a Rust-written one read the same to every reader. Since 2026-09-20 the Rust leg stamps the two writer keys at the shared builder. `removed_by` carries the bounded argv verb. `removal_trigger` reads `unattended` from the daemon binary and `session` from anything else. The `Writer` parameter makes an unnamed receipt unconstructible. The Python leg still writes the argv0 basename into `removed_by`. It writes no trigger, so its receipts read as unknown-trigger, the same treatment the pre-stamp tail gets. Once Python `update_registry` routes its removal accounting through the Rust choke point, the port deletes the Python leg. The same PR retires the empty-trigger clauses in the feed and verify readers. No parity guard is being written for it either. The door-inventory guard in `crates/fno/tests/cross_door_property.rs` already fails a hand-written unstamped receipt. A second guard can only buy the Python leg tenure it has not earned. Recorded, not taken here.

**The macOS process-table readers.** Dual, disposition dual-logic, port owed, not in this PR. Rust leg `snapshot_macos` in `crates/fno/src/process_admission.rs`: the admission census, which answers "may this spawn proceed" and refuses the whole reading over one unreadable row it can name as ours. Rust twin `process_table_libproc` in `crates/fno-agents/src/census.rs`: the fleet census, which answers "what is on this machine", counts an unreadable row and carries on, and never gates a spawn. Both walk the same libproc table through the same syscalls with opposite error policies, and both are correct for their own question. A port here deletes an error policy, and each policy is the answer, so a port changes who gets admitted. Recorded, not taken.

**The active-tail vocabulary.** Dual, disposition shared-vocabulary, port owed, not in this PR. The canonical leg is `_ACTIVE_STATES` in `cli/src/fno/agents/reachability.py:156`, read by `classify_reachability`. Eight sites spell the same three words by hand, across eleven lines. `discover.py:1100` is an addressing filter by design. `dispatch.py:8745`, `post_merge_route.py:144`, and `watchdog.py:1445` are state-only consumers. `relay/registry.py:231` and `:290` add `unknown` to the set, so they ask a different question. `client_verbs.rs:2042` and `:2132` read the lowered state word. `king_board/classify.rs:16` is a copy already pinned to the Python text by a parity test at `classify.rs:402`. `truth_probe.rs:237` and `:281` are monotone lowering, which cannot raise a state to live. After 2026-09-09 all eight inherit the expiry that `classify_tail` applies to the word itself, so none of them can read a week-old tail as active. The residual is the band between `TRANSCRIPT_EVIDENCE_S` and `stalled_after_s`: no unported site holds an age, so the tighter bound needs new plumbing. A port moves each site onto `classify_reachability` with the age it already had to fetch.

**Process-liveness readers.** Dual, disposition dual-logic, port owed, not in this PR. Python leg `census` in `cli/src/fno/agents/spawn_gate.py`: the `fno agents top` audit surface only - the Python admission gate's own reading is gone: the one gate counts its own slots in Rust, and `census` survives for the display surfaces (top, orphans, unfinished work). It reads psutil pid plus start time, the claude roster pid, a live worker slot claim. Rust twin `slot_count` at `crates/fno-agents/src/spawn_gate.rs`, pinned to the Python census by the shared fixture in `cli/tests/agents/test_spawn_gate_agreement.py`. The sweep leg is `plan_reconcile` in `crates/fno-agents/src/liveness_sweep.rs`. Its served pair reads the freshness rule in `crates/fno/src/served_liveness.rs`. The rule is vendored into fno-agents and pinned by a contract test. A real dependency waits on a synchronized 0.3.x release: `cargo publish` resolves the dependency against the registry copy, which predates the module. Both gates answer "is there fleet headroom" from their own process reading. The sweep measures the same rows, so a saturated machine can make the two disagree by construction. The port moves both admission gates onto the served measurement, and that is gate semantics: who gets admitted on a saturated machine changes. It is recorded as owed, not taken here. After this port, `fno agents registry-json` is also a consumer of the served measurement. The Rust client derives the two served fields from the same freshness rule. Both counting hooks read that verdict instead of running their own status-word filter.

**pr-watch liveness readers.** Dual, disposition dual-logic, port owed, not in this PR. Python leg `liveness_report` in `cli/src/fno/pr_watch/_install.py`, read by `fno do pr watch status`, `status --json`, the SessionStart hook, `fno doctor` and `_observer_health` in `cli/src/fno/pr/_status.py`. Rust leg `explain_inner` in `crates/fno-agents/src/tick_ledger.rs`, the arms table's pr-watch verdict. Both fold `pr_watch_tick_end` records into "is the watcher delivering". On 2026-09-15 they disagreed: the Rust leg read FAIL and the Python leg read healthy-pending while every recent tick ended broken. A port deletes the Python leg once its five consumers read the verdict through `fno-agents`.

**The dispatch-agent naming vocabulary.** Dual, disposition dual-logic, RETIRED 2026-09-16 (PR 1813). The Rust leg `crates/fno-agents/src/naming.rs` owns the tables, the mint, the parse, and the provenance rows. The Python legs were second spellings of its answers, and this PR deletes them. `cli/src/fno/agents/truth_status.py` loses the `_NAME_NODE_RE` and `_NAME_MISSION_RE` legacy regexes. `cli/src/fno/agents/naming.py` loses `legacy_verb_code`. `cli/src/fno/autonomy_cli.py` loses `audit_dispatch_provenance` and the hidden `autonomy provenance` verb. The audit's CI caller now runs `fno-agents name-codes --check` directly. The port teaches the binary parser the pre-cutover `target-`/`think-` read window so the regex fallbacks die with an owner in place. Surviving Python surface, with callers, so nothing is deleted quietly: `verb_code_for` and `accepted_verb_words` (`node_dispatch.py:90,124`, `dispatch_overrides.py:31,44`), `agent_name` (the legacy prefix form), and `bridge_name` (`cli.py:2828`). That word-to-code surface is its own port, owed, not in this PR. The `autonomy status` SOURCE/VERB columns keep reading the rows through the thin binding.

**The events write boundary.** Not dual-logic: shared-vocabulary, eight writers stamping one envelope field, port owed, not in this PR. Eight sites stamp a `source` onto an events.jsonl row. Exactly one judge validates it, and only one of the eight consults that judge. The judge is `validate()` in `cli/src/fno/events/__init__.py:295`. Since 2026-09-16 it enforces a type's declared `sources:` list from schema.yaml, not only the envelope-wide enum. The eight writers, named. `append_event` (`cli/src/fno/events/__init__.py:1839`) is the one leg that calls the judge before an event lands, across roughly 40 Python call sites. The `fno doctor event emit` CLI (`cli/src/fno/events/cli.py`) builds through `append_event` too, but earns its own name because its `_detect_source` decides the producer identity the judge then checks. `EventEmitter::write_line` (`crates/fno-agents/src/events.rs:149`) carries no validator. `king_checkin.rs::emit_row` (`crates/fno-agents/src/king_checkin.rs:1346`) now routes through `EventEmitter`, since the reign_checkin writer unification in PR 2073. `claude_ask.rs::emit_event` (`crates/fno-agents/src/claude_ask.rs:1987`) writes the retired `{ts, kind, flat}` shape with no `type` and no `source`, shared by the claude/codex/gemini/agy/opencode ask paths. `scripts/lib/events.sh` contributes two: `emit_event` (line 242) and `emit_event_raw` (line 266). With an empty third argument, `emit_event_raw` drops `source` entirely. Two local hook shims round out the eight: `hooks/context-nudge.sh:69` and `hooks/agy-target-stop-hook.sh:218`. A 2026-09-15 measurement covered 936,975 rows in 6,033 journals. 6,635 rows carried a source their own type forbade, across 14 distinct pairs. Each pair is now reconciled. The honest side won, either the live producer or the declaration. 5,834 rows carried no `type` at all, the retired ask shape. 231 `review_attestation` rows carried no `source`, the bash `emit_event_raw` gap. The port direction: the judge moves to where every writer can reach it, or the seven other writers route through it. No parity guard is being written for it, on purpose, matching the receipt-handle row above. Writing one is the trigger to port instead. A guard here can only buy the unvalidated legs tenure they have not earned.

**The resume message lane.** Dual, disposition dual-logic, port owed, not in this PR. When a claude row that `claude agents` lists as `blocked`, `done`, `stopped` or `failed` takes `-m`, the Rust leg `parked_claude_route` in `crates/fno-agents/src/resume_wake.rs` delivers the text itself. When the row's daemon worker is gone, the route revives the session in place first. The Python leg `_resume_claude_wake` in `cli/src/fno/agents/resume_cli.py` keeps the bare wake and is the only leg a caller pinned to `FNO_AGENTS_RUNTIME=python` reaches. It refuses a `done` row. The port moves the Python wake onto the Rust inject.

**The claude stop receipt.** Dual, disposition dual-logic, port owed, not in this PR. The Rust leg is `stop_claude` in `crates/fno-agents/src/daemon/claude_stop.rs`. It proves the worker's process set from claude's own daemon roster, ends any survivor itself, and answers `stopped` with a process proof behind it. The Python leg is `cli/src/fno/agents/stop_receipt.py` with the escalation at `cli/src/fno/agents/dispatch.py`. It refuses or escalates on the registry pid, and runs only under `FNO_AGENTS_RUNTIME=python` or with no installed binary. No parity guard is being written for it, deliberately, matching the receipt-handle row above. When the last python-runtime caller is gone, a port deletes the Python leg.

Nothing else in the tree is a confirmed dual implementation today.

**The status readiness conjunction.** Dual, disposition dual-logic, RETIRED by the one-readiness-function port (2026-09-20). The Python leg was `cli/src/fno/pr/_status.py` `_ready_blockers` plus its ad hoc appends: dispatch hold, review activity, GitHub merge state, the waiver overlay, the rerun append. It computed `ready` with its own conjunction, while `fno do pr merge` decided with a different chain. The legs answered opposite ways repeatedly. Status read `ready: true` on PRs the merge verb held (`ci_base_stale`, `merge_slot_held`), on a MERGED PR (`pr_terminal`), and on a base-stale slot holder. Each case was discovered one merge refusal at a time. The port makes `crates/fno-agents/src/authorized_merge.rs` `Effect::Preview` the ONE merge readiness decision. Status asks it, reads `ready` and `ready_blockers` off the receipt, and the receipt rides as `merge_decision`. The Python merge gates (coverage refusal, stub manifest, plan fidelity, overlap, flake hold) moved into `crates/fno-agents/src/merge_gates.rs`, so merge and status share one gate list. Deleted with the Python leg: `_ready_blockers`, the waiver overlay copy, and `unreconciled_manifest_for_pr` with its node lookup. Also deleted: the overlap probe helpers in `_merge.py` (`_live_lane_count`, `_behind_by`, `_base_move_paths`, `_overlaps`) and the merge lock's inline lineage read. `_pr_base_head_refs` and `_pr_file_paths` survive with non-gate callers. The status-side cache key moved to the owner's `status-cache-key` op, so a hold release, a slot move, or a merge rekeys the row. Surviving transports, so nothing is deleted quietly: `_merge_hold_reason` and `_github_merge_blockers` stay as receipt facts. `_ci_blocker_word` moved to Rust beside the gate (`ci_blocker_word`). `_review_lane` stays as the coverage recompute cost guard, the same `_merge._review_lane_configured` predicate the gate reads.

## The eight CI parity scripts

All eight are shared-vocabulary. None is a port candidate. The reasons differ enough to state one at a time.

| Script | Compares | Verdict |
|---|---|---|
| `check-coverage-context-parity.sh` | two commit-status context strings and one label, across the Python publisher, the operator ruleset data, the Rust publisher, and the refresher workflow | shared-vocabulary |
| `check-harness-roster-parity.py` | `KNOWN_HARNESSES` against three evidence surfaces: `docs/SETUP-<name>.md` filenames, the `for_name()` match arms in `provider.rs`, and the `_register()` calls in the Python adapter registry | shared-vocabulary |
| `check-provider-vocabulary-parity.sh` | provider vocabulary across four Rust files and two Python files | shared-vocabulary |
| `check-review-app-parity.sh` | review-App logins across `BOT_PROFILES` in Rust and two Python declarations, plus a `usage_markers` field per profile | shared-vocabulary |
| `check-reviewer-descriptor-parity.sh` | `_RESOLVABLE_REVIEWERS` against `REVIEWER_INVOCATIONS`: invocation string, self-cert flag, per-harness verb overrides | shared-vocabulary |

One of these needs a note. A reader in a hurry can mistake it for a port candidate.

`check-coverage-context-parity.sh` and `check-harness-roster-parity.py` both compare code against artifacts that are not code. A GitHub workflow, operator ruleset data, and setup-doc filenames are among them. No port retires either one.

`check-reviewer-descriptor-parity.sh` is the closest of the five to a port candidate. It compares one declaration set held twice, in two languages. A port of the config layer deletes one side. It is still not dual LOGIC, and the config layer is not on the sequence below.

## The port protocol

Use this. Do not invent another. The repo proved it on two migrations. The property that makes it good is simple. The safety net survives the deletion instead of dying with it.

0. **Measure both budget axes.** Count the seam crossings and the dual set before step 1. A port that raises either axis is refused, however clean its diff. The reason and the gaming path are in [the seam doc](rust-python-seam.md). This page's hand list is the second axis until the duplicate-discovery sweep lands.
1. **Differential parity while both legs exist.** Run both implementations over identical fixtures and assert byte-equality.
2. **Capture goldens from the old leg first.** Do this before touching it, while it is still proven correct. Under `FNO_CAPTURE_GOLDEN=1` the helper runs the old leg, writes the goldens, and asserts new equals old before freezing. Goldens live at `crates/fno-agents/tests/golden/<subject>/<case>.{exit,out,err}`, keyed by a slug of the case label.
3. **Delete the old leg and move its callers.** Both, in one change.
4. **Convert the parity test to a characterization test.** It asserts against the goldens from step 2.

Never delete a leg before proving it unreachable. The ask adapters are live in production. A port is a migration with callers to move, never a cleanup. `verify_evidence` was free only because its counterpart was already gone.

When the other language is unavailable, a test skips. It never fails. The live-differential residue in `claude_ask_parity.rs` and `codex_ask_parity.rs` shows the shape. It covers the spawn substrate whose Python counterparts survive. The golden-driven cases need no other language at all.

## The provenance declaration

Every `*_parity.rs` declares its stage and its oracle in its header. `scripts/ci/check-parity-test-provenance.sh` asserts that declaration against the filesystem, in both directions.

```rust
//! parity-stage: differential
//! parity-oracle: fno.agents.harnesses.claude
```

`parity-stage` is exactly one of `differential` or `characterization`. `parity-oracle` names the LEG, in one of three forms.

- A repo-relative path, for a leg that occupied a whole file.
- A dotted module, for a leg that occupies a whole Python module.
- A dotted module with one final symbol, for a leg inside a surviving module. The symbol names a `def`, a `class`, or an assignment at that module's top level.

The symbol form is the ordinary Python case. The module file survives the port, the ask functions inside it do not, and only a symbol can say which leg is gone. The two finished bash ports made "the file is gone" and "the leg is gone" read as one sentence. They shared that property by accident, and no Python oracle shares it at all.

The check is two-sided on purpose. A `characterization` file must name an oracle that NO LONGER RESOLVES. The leg was deleted, and the golden stands in for it. A `differential` file must name one that DOES resolve. A live second implementation is the only reason to run both legs. Either way the assertion has a positive marker rather than an absence. Either way it fails loudly the moment a port finishes or a new dual implementation appears.

The header carries no node id and no PR number. `scripts/ci/check-no-internal-refs.sh` fails on the node id. The PR number is house style, and no gate catches it. The oracle is the identity. A path names a whole-file leg, a symbol names a leg inside a surviving module, and resolution is what both arms of the check test.

## Sequence

The claim classifier went first and is complete. It was the only dual implementation with no parity guard when this sequence was filed; its golden characterization now proves the Rust decision is the sole surviving leg.

Retirement order is subordinate to the crossing dependency order in [the seam doc](rust-python-seam.md). A leg with no guard still waits for the legs it feeds. Risk argues for a port. Dependency decides the safe order.

The ask adapters went second, and their retirement is complete. They sat at step 1. The work was steps 2 through 4, plus moving one caller and fixing the guard the first symbol-oracle port exposed.

The graph store went third (2026-09-02). It followed the same four steps. The goldens came from the Python leg. One change deleted the Python store core and moved its callers onto the keeper client. The parity file is now characterization. Two of its cases are Rust-only: the corrupt-kind taxonomy and the concurrent-writer case. The Python leg never held those behaviors, so no golden exists for them.

There is no third. Do not open a `kill_criteria` port under this sequence. The table above removed it from the dual set. Filing one re-imports the error this page corrects.

One deferred item sits in the family without joining the sequence, and its reason generalizes. The tier-remap refusal exists in the Python spawn launcher and not in the Rust spawn binary. That is a guard MISSING from a second implementation. It is not a second implementation to retire. Writing that guard again, in the second language, commits the exact pattern this page removes. When the spawn entry point collapses to one implementation, the gap closes for free. A missing guard on a duplicated entry point argues for finishing the port, never for duplicating the guard.
