# Grade-only reachability regression: each fresh fixture must retain every
# active capped AGENTS.md pitfall in the Claude, Codex, and Gemini kernels.
id: regression-context-pitfalls-reachable
tier: regression
grade:
  - kind: exit
    command: "cd cli && uv run pytest tests/unit/test_context_audit.py -q -k required_pitfalls_reachable"
    expect: 0
timeout_minutes: 5
tags: [context, reachability, pitfalls]
