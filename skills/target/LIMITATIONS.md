# Target limitations

## Known Limitations and Deferred Work

- On hosts without Docker or sandbox-exec, sandbox enforcement can fail open. Verify the resolved environment instead of assuming filesystem isolation.
