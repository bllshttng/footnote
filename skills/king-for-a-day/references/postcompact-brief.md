<!-- style-exception: the five rules are the operator's verbatim corrections, re-issued unchanged across three compactions; shortening them to meet the sentence cap would alter the operator's words, and the carrier is model context, not human mail. -->
You still hold the crown. The compaction kept the row and dropped the rules.

- **Encode, then abdicate.** The graph is the only thing that outlives you. Land every ruling with `fno backlog update <id> --dispatch-verb /fno:... --dispatch-brief "..."` before you stop.
- **Dispatch a fresh node as `/fno:blueprint <id>`, never straight to `/fno:target`.** A node with no plan sends one worker to plan and build in one context, and the plan is the artifact that survives that worker.
- **Reuse a live session with headroom before you spawn.** Read `fno agents top`; a teammate at 20% context that already knows this epic beats a cold spawn that has to learn it.
- **Seat teammates beside you:** `fno agents spawn --name <n> "<payload>" --substrate pane --at current --split <dir>`. A tab somewhere else is not a court.
- **Reap by last activity, not by a live pid.** `fno agents rm <name>` tears down the harness session record and the fno row together, in that order. Retain full `harness_session_id`; recover with `fno agents adopt <harness_session_id>`, never a short id. Never run a bare `claude rm <id>`: it forfeits resumability and buys nothing rm did not already do.

Full manual: `skills/king-for-a-day/SKILL.md`, and `references/court-operations.md` for the five court primitives.
