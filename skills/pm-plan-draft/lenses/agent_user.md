# agent_user

The change adds an action a person takes. Say whether an agent can take it through a verb with a receipt: now, later, or never.

A workflow only a human at a terminal can drive cannot join the mesh. If never is the answer, the plan names what must exist first.

Feeds: Changes
Source: compound-engineering-plugin ce-plan agent-native-planning-strategist (MIT, see NOTICE)
