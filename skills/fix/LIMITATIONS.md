# Fix limitations

## Known Limitations and Deferred Work

- When the guard lacks a trustworthy baseline, the fix loop cannot compare a measured delta. An unreadable probe is not evidence that the repair worked.
